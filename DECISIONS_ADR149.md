# ADR-149:零股在**既有下單路徑**上也用零股五檔;即時進場補上進場時間閘門

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-147(定時下單 / `core/quote_book.py`)、ADR-145(即時進場)、
> ADR-146(零股機制稽核)、ADR-135(停損停利即時觸發)、ADR-035(樂觀成交模型)、
> ADR-066(進出場時間窗)、P-06、P-64、P-67、P-128
> 新增坑:P-131

---

## 起因:ADR-147 只接了一條路

ADR-147 解掉了「12:01 用當時的五檔下一張零股」,做法是新增一條 opt-in 的
**定時下單**路徑,取價規則放在 `core/quote_book.py`。那條路是對的,也已經
在 `main` 上。

但零股策略走**其他**路徑下單時,價格來源仍然是整股:

| 路徑 | 取價來源 | 為什麼一定是整股 |
|---|---|---|
| K 棒進場/出場 `_quant_eval_pass` | `b_df['Close']` | **零股沒有 K 線**,拿得到的只有整股 K 棒 |
| 即時進場 `_qt_check_realtime_entries` | `snapshots()` 的 `close` | shioaji 的 snapshot **只有整股欄位**(P-06) |
| 即時停損 `_qt_check_realtime_futures_stops` | 同上 | 同上 |

`_qt_get_book()` 在合併前全檔**只被呼叫一處**(定時下單),這件事本身就是證據。

### 為什麼要修

零股依交易所規則只能限價(鐵則 6),所以「掛什麼價」就等於「會不會成交」。
基準價取自另一個市場,買單可能掛在零股賣方報價之下,整天掛著不成交 ——
而系統採**樂觀成交模型**(ADR-035):委託送出成功就 `apply_fill`。於是:

> **策略以為自己買到了,實際上一股都沒買到。**

而且 ADR-135 之後,零股的停損停利也走即時通道,用的是 `snapshots()` 的整股價
去對比進場成本 —— 一旦進場價改記零股價,兩者就是「用 A 市場的現價減 B 市場的
成本」,損益從第一筆就錯。

---

## 順帶修掉:即時進場通道**完全沒有**套用使用者設的進場時間

`filter_intents_by_time()`(進/出場時間窗,ADR-066)只長在 **K 棒路徑**上。
ADR-145 新增的即時進場通道 `check_realtime_entry()` **一行時間判斷都沒有**。

> 使用者設「只在 09:30~11:00 進場」,只要那檔策略同時勾了即時進場,
> 這個限制就等於不存在,**任何時間都可能被開倉** —— 而且畫面上看不出來。

這比「條件到了卻沒動作」更嚴重:前者只是沒賺到,後者是**在他明確不想下單的
時間下了單**,而且是真錢。

---

## 決定

### 1. 一律沿用 `core/quote_book.py`,**不新增第二個取價模組**

這是這一筆最重要的架構決定。兩個模組同時決定真錢的掛價,遲早分歧(P-67),
而分歧的後果是委託價格。診斷裡有一條**反向對照**直接斷言
`core/odd_lot_price.py` 不存在 —— 那是本筆的前身留下的檔名,寫死在斷言裡是
為了讓「又開一個第二模組」當場紅。

### 2. 常駐訂閱範圍:定時下單 ∪ **零股策略**

`_qt_timed_targets()` → `_qt_book_targets()`(改名,因為涵蓋範圍已不只定時下單),
`_qt_sync_timed_subs()` → `_qt_sync_book_subs()`。訂閱條件從
「已啟用 + 有開定時下單」擴大為「已啟用 +(有開定時下單 **或** 是零股策略)」。

停用的策略不佔配額、期貨不在範圍,這兩點沿用 ADR-147 沒有變。

### 3. 進場取不到五檔 → **不送單**;出場取不到 → **退回整股價照樣走**

**這個不對稱是刻意的,而且是整筆最需要講清楚的一條。**

| | 取不到五檔時 | 理由 |
|---|---|---|
| 進場 | **不送單** + 記日誌 | 錯價單會掛不到,而樂觀成交模型會讓策略以為成交了 —— 部位認知從此對不上。沒送出去只是這一輪沒進場,下一根 K 棒/下一個 tick 還會再評估 |
| 出場 | **退回整股價**,照樣送 | 出場沒送出去是**部位裸奔**。停損走不掉是這個系統裡最糟的一種失敗,寧可用一個偏掉的價也要走得掉 |

進場的選擇與 ADR-147 的定時下單一致 —— 同一個系統對「取不到五檔怎麼辦」
不可以有兩種答案。

> **誠實說明**:本筆的前身(我在工作分支上寫、後來作廢的那版 ADR-147)選的是
> 「進場也退回整股價」。改成對齊 ADR-147 的理由有兩個:錯價單的傷害大於延後
> 進場;而且兩種答案並存本身就是缺陷。

### 4. 出場的觸發價與掛單價都用**對手價**

停損問的正是「如果我現在出場,拿得到什麼價」—— 對手價(賣出看買方、買進看
賣方)就是那個答案。用成交價或中價會**高估**你出得掉的價,停損就會晚跳。

### 5. 既有路徑一律用**第 1 檔**,不新增旋鈕

「要多積極」已經由既有的 `slippage_ticks`(讓價檔數)表達;`timed_book_level`
是為「我指定時刻、也指定積極度」那個場景設計的。多一個旋鈕就是多一個會設錯
的地方,而第 1 檔正是最不容易掛著不成交的。

### 6. 進場時間閘門抽成 `entry_time_gate()`,兩條路共用

- `filter_intents_by_time()` 的 OPEN 分支改呼叫它(K 棒時間)。
- `check_realtime_entry()` 新增 `now_hhmmss` 參數並套用它(牆上時鐘)。
- GUI 呼叫端**也**先問一次 —— 不是重複,是為了寫日誌(被擋要講得出原因)。

**刻意不改 `specific_entry_time` 的語意。**它比對「時刻完全相等」,是 ADR-066
定下、ADR-147 明確決定不動的既有行為。即時通道的節拍是秒級,實務上不可能命中
某一秒,所以設了它的策略在即時路徑上會一直被擋 —— 那是**正確**的:使用者要的
是「就在那一刻」,而這條路給不了保證。

所以日誌會**主動指路**:

> 即時進場的節拍是秒級,無法保證命中某一秒;要在指定時刻進場請改用「定時下單」
> (有明確的窗口設定)。

不要讓使用者以為是 bug,而是告訴他對的工具在哪。

---

## 我改了三處別人的既有斷言 —— 要講清楚

這一類改動最容易變成「改測試讓它過」,所以逐條說明。

### (1) ADR-147 案例:「沒開定時下單就不該訂閱五檔」

規則變了(訂閱範圍擴大到零股策略),而那個案例的 `_mk()` 預設就是零股,
所以舊斷言必然紅。**要守的意圖沒變**:不該為用不到的策略佔配額。
反向對照換成「既沒開定時下單、也不是零股」的股票策略,並**新增一條正向對照**
釘住 ADR-149 的新行為(零股策略即使沒開定時下單也要訂閱)。

### (2) ADR-143 案例:零股子案例現在需要五檔

零股進場改成「取不到五檔就不送單」之後,那個案例沒餵五檔,零股整檔不進場。
**原始斷言一字沒改** —— 我把五檔的買一與賣一**都**設成 600.0(= B 的價),
所以進場(吃賣一)與出場(吃買一)取到的都是 600,它原本要守的
「下單價要用 B 的價,不是 A 的 97」完全不受影響。

順帶補了 `finally` 還原 `_qt_books`:五檔快取是全域的,不還原會把狀態留給
後面的案例。

### (3) `_qt_timed_targets` / `_qt_sync_timed_subs` 改名

純改名(涵蓋範圍變大之後原名會誤導),ADR-147 案例裡 4 處引用同步更新,
意圖與斷言內容都沒有變。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/strategy_engine.py` | 新增 `entry_time_gate()`;`filter_intents_by_time()` 的 OPEN 分支改走它;`check_realtime_entry()` 新增 `now_hhmmss` |
| `stock_app_pro.py` | `_qt_book_targets()` / `_qt_sync_book_subs()`(改名+擴大範圍);新增 `_qt_odd_entry_price()` / `_qt_odd_exit_price()` / `_qt_odd_log_once()` / `QT_ODD_BOOK_LEVEL`;三條下單路徑接上;即時進場補時間閘門 |
| `tests/test_core.py` | +9(`TestEntryTimeGate`) |
| `diag_repro_issues.py` | +1 案例(ADR-149);ADR-147 案例的訂閱斷言更新+新增正向對照;ADR-143 案例餵五檔+還原狀態;改名同步 |

**沒有**新增任何 `core/` 模組 —— 這是刻意的(見決定 1)。

---

## 驗證

離線(全部通過):

- `python tests/test_core.py` → **904 案**(`main` 原 895,+9)
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → **71 案例**(`main` 原 70,+1)
- `python diag_crossref.py` → 無跨模組斷鏈或重複定義
- `py_compile` 全過

---

## 一個沒能歸因的偶發紅(誠實記錄)

開發過程中 `ADR-133`(主圖盤勢判斷自動掃描)出現過**一次**
「同一格不該重抓,實際又抓了 ['日K', '60分K']」。之後:

- 在本分支連跑 **5 次全綠**;
- 用 worktree 在**乾淨的 `main`** 上連跑 **5 次也全綠**。

也就是說,它在改動前後都沒能重現,**我無法歸因**。程式碼層面查過:
`_regime_scan_slot()` 在傳入固定 `now_dt` 時是純函式、完全決定性,而 slot 是在
任何可能拋例外的呼叫**之前**就寫入的 —— 找不到會讓兩個週期同時漏記的路徑。
ADR-149 的改動也沒有碰到那條路徑上的任何東西。

依 P-94:**「跑很多次都沒紅」不是證明,只是沒有反證。**所以這裡只記錄觀察,
不宣稱它已修好、也不宣稱它是既有問題。下次再出現時,這段記錄可以省下重新
懷疑一輪的時間。

(過程中順手修掉一個真的狀態外洩:ADR-143 案例寫了全域的 `_qt_books` 卻沒有
還原。那不足以解釋上面的現象,但本來就該修。)

---

## 請使用者實機驗證

1. **啟用一檔零股策略(不必開定時下單)**,系統日誌要出現
   `【策略報價】已訂閱 2330 的零股五檔 (常駐)`。沒有這一行,後面全部會退回。
2. **盤中零股進場**,日誌掛價要標 `[零股五檔 買進第1檔 xxx]`;
   標不出來就代表沒拿到五檔(那時候應該是**不送單**並說明原因)。
3. **反向對照(最重要)**:持有零股部位時把停損設得很近 → 停損要**走得掉**。
   即使串流斷掉(拔網路/盤後),也應該退回整股價出場,並在日誌標
   `[整股價 (...)]`。**出場被擋住是嚴重 bug,請特別看這一項。**
4. **零股停損的觸發時機**:應該以**買一**(你真的賣得掉的價)判斷,
   而不是賣一或整股價 —— 表現是「比舊版更早一點觸發」。
5. **即時進場 + 進場時段**:設一個現在不在範圍內的進場時段,勾即時進場 →
   不可以開倉,而且日誌要講一次原因(每檔每天一次)。
6. **設了「特定進場時間」+ 勾即時進場** → 日誌要出現指路訊息,
   告訴你改用「定時下單」。
7. **反向對照**:一般**股票**策略的掛價不可以有任何變化(仍用整股 K 線收盤價),
   而且不會因為沒有零股五檔就被擋下來。
8. **定時下單(ADR-147)本身的行為不可以改變** —— 它仍然走自己的
   `timed_book_level` 與窗口設定。
