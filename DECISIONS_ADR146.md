# ADR-146:零股機制稽核 —— 09:10 開盤、只能限價、數量 1~999 股

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:鐵則 6(零股只能限價 ROD、1~999 股)/ 鐵則 9(送出前本地驗證)/
> ADR-013(數量上限)/ ADR-072(零股開盤時刻可設定)/ ADR-124(時段閘門)/
> ADR-145(即時進場)
> 新增坑:P-126

---

## 起因

> 「我用零股下單,可下單時間是09:10之後,零股只能用限價單。檢查這個機制是否有問題。」

逐項查證的結果:**兩項對、一項有真 bug**。

---

## 稽核結果

### ✅ 一、可下單時間 09:10 之後 —— 正確

`market_session.is_odd_lot_open()`:週一~五、`ODD_LOT_OPEN_MIN`(預設 09:10)
~ 13:30。逐分鐘實測:

| 時間 | 零股 | 整股 |
|---|---|---|
| 09:00 | ❌ | ✅ |
| 09:09 | ❌ | ✅ |
| **09:10** | **✅** | ✅ |
| 13:30 | ✅ | ✅ |
| 13:31 | ❌ | ❌ |
| 週六 | ❌ | ❌ |

而且**三條會下單的路徑**都吃同一道閘門:`_quant_eval_pass`(K 棒)、
`_qt_check_realtime_futures_stops`(即時停損)、
`_qt_check_realtime_entries`(ADR-145 的即時進場)。診斷用原始碼切片逐一斷言。

開盤時刻可設定(ADR-072),未來交易所改成 09:00 時不必改程式。

> ⚠️ 唯一的例外是使用者自己把策略的 **`session_gate` 關掉** —— 那是明確的
> 選擇(「不管時間都跑」),程式尊重它,但 09:10 前送出去一定會被券商退。

### ✅ 二、只能限價 —— 正確,而且是兩層防線

1. `strategy_engine.price_type_of()` 開頭就對零股回傳「限價」,不管策略存了什麼。
2. `order_intent.build_live_order()` 在交給 adapter 之前**再擋一次**。

存檔時 `validate_price_type()` 也會擋(「零股依交易所規則只能限價」)。
診斷驗過「策略被改成市價,送出去的仍是限價、而且價格不是 0」。

### ❌ 三、數量上限 —— **真 bug,已修**

`validate_strategy()` 的數量檢查是:

```python
if q > 100:
    return False, "單筆數量超過 100 (防呆上限),請確認後調整程式碼上限"
```

那個 100 是**「張 / 口」**的防呆上限。但**零股的單位是「股」**,
交易所規則是 **1~999 股**。後果有兩個,方向相反:

| 情境 | 後果 |
|---|---|
| 想買 **500 股**(完全合法) | **被擋下**,而且訊息叫使用者「請確認後調整程式碼上限」—— 把人引導去改一個**不該改**的常數 |
| 真的有人照訊息把 100 改大 | `build_live_order()` **完全不擋** 999 上限 → 5000 股的零股單會直接送去被券商退 |

而 `core/order_rules.MAX_QTY_ODD = 999` 這條**正確的規則早就存在**,只是
那份只給「手動下單對話框」用,策略路徑沒有用到 —— **同一條規則兩份,
而且策略那份是錯的**(P-67 的經典案例)。

---

## 修法

### 上限收斂成單一出處,並且分清楚「誰的規則」

`core/order_rules.py` 新增:

```python
MAX_STRATEGY_QTY_LOT = 100            # 策略的整股/期貨,單位:張/口 (本系統防呆)
MAX_STRATEGY_QTY_ODD = MAX_QTY_ODD    # 策略的零股,單位:股 (交易所規則,與手動同一份)

def strategy_qty_limit(trade_type):
    """→ (上限, 單位, 是不是交易所規則)"""
```

第三個回傳值是刻意的:**交易所規則不可以叫使用者「去改程式碼上限」**。
現在的訊息會分兩種說法:

```
零股 1000 股 → 零股單筆數量須為 1~999 股 (交易所規則;超過請改用整股下單)
股票  101 張 → 股票單筆數量超過 100 張 (本系統的防呆上限,不是交易所規則)
```

> 手動下單與策略下單的上限**刻意不同**:手動是人盯著按的,策略是無人看管、
> 一天可能觸發好幾次,所以整股/期貨壓得更保守(100 vs 499)。
> 但**零股那條是交易所規則,兩邊必須一致** —— 那正是原本出錯的地方。

### 送出前再擋一次(鐵則 9)

`order_intent.build_live_order()` 加一道數量檢查。兩道都要有的理由很具體:
策略存檔之後,數量還可能經由**舊策略檔的殘留值、手動改 JSON、日後新增的
批次調整**變動,而這裡是所有委託離開系統的**最後一個關口**。

零股超過 999 一定被券商退,而被退的當下策略已經以為自己成交了
(ADR-035 的樂觀模型)—— 部位認知就此對不上。

---

## 順帶查到、但**沒有問題**的兩件事

- **零股不可做空**:`validate_strategy` 擋掉(「做空僅支援期貨」)。✅
- **`unit_size('零股') = 1`**:回測與模擬帳戶的金額換算是以「股」計,正確。✅

## 一件已知的近似,這次沒改

看A做B 時 B 是零股的話,即時價與 K 棒都取**整股**的資料 —— 盤中零股是獨立
市場、有自己的成交價。但零股沒有獨立 K 線可用,而且既有的 K 棒路徑本來就是
這樣,所以這是**既有的近似而不是這次的迴歸**。實務上同一檔股票兩邊價格很接近,
影響的是限價單的掛價基準。要改的話是另一筆 ADR(需要決定零股價格的來源)。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/order_rules.py` | 新增 `MAX_STRATEGY_QTY_LOT` / `MAX_STRATEGY_QTY_ODD` / `strategy_qty_limit()` |
| `core/strategy_engine.py` | `validate_strategy` 的數量檢查改用 `strategy_qty_limit()`;訊息分「交易所規則 / 本系統防呆」 |
| `core/order_intent.py` | `build_live_order()` 加送出前的數量守門(鐵則 9) |
| `tests/test_core.py` | +13 |
| `diag_repro_issues.py` | ADR-146 案例(走完整下單路徑);新增 `_REAL_IS_MARKET_OPEN` |
| `PITFALLS.md` | P-126 |

**編號檢查**:本地與 `origin/main` 都查過。目前最大 ADR-145、最大 P-125。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **865 通過**(原 852,+13)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **67 案例全 PASS**(原 66,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| 零股上限退回舊的 100(重現使用者的問題) | 單元紅 + 診斷紅 |
| 整股上限被放寬成 999 | 單元紅 + 診斷紅 |
| 送出前不擋數量(鐵則 9 失效) | 單元紅 + 診斷紅 |
| 零股不再強制限價(鐵則 6 失效) | 單元紅 + 診斷紅 |

### 一個寫診斷時踩到的坑(P-126)

第一版的時段斷言全部是**空殼**:`run_case` 為了防時鐘相依(ADR-126)會把
`market_session.is_market_open` 凍結成**永遠回 True**。所以在案例裡呼叫它去驗
「09:09 不該開盤」,拿到的一定是 True —— 而它剛好讓斷言紅了,我才發現。
若我當初把斷言寫成反向(「09:10 要開盤」),它會**永遠綠**,而且看起來完全合理。

修法:在 `run_case` 定義**之前**抓一份未被凍結的參考
(`_REAL_IS_MARKET_OPEN`),要驗時段規則本身的案例一律用它。

### 請使用者實機驗證

1. **開一檔零股策略,數量填 500 股** → 應該**存得起來**(這是這次修的主體;
   修之前會被擋,而且訊息叫你去改程式碼)。
2. 數量填 1000 股 → 應被擋,訊息是
   「零股單筆數量須為 1~999 股 (交易所規則;超過請改用整股下單)」。
3. 整股策略填 101 張 → 應被擋,訊息是
   「股票單筆數量超過 100 張 (本系統的防呆上限,不是交易所規則)」——
   兩種訊息的**說法不同**,這是刻意的。
4. 零股策略把委託方式選成「市價」→ 應該**存不起來**;就算硬改 JSON,
   送出去的仍是限價(日誌會顯示「限價XXX x500股」)。
5. **09:10 之前**啟用零股策略 → 系統日誌應顯示休市待命,不會送單;
   09:10 一到自動開始評估。
