# ADR-154:指定時刻是「從這裡開始看」,不是「只看這一眼」

- **狀態**:已實作,離線驗證通過,**未經使用者實機驗證**
- **日期**:2026-08-07
- **關聯**:ADR-147(定時下單)、ADR-151(回測要知道定時下單)、
  ADR-153(定時下單看「當下」不看「曾經」)、PITFALLS P-136

---

## 1. 起因

ADR-153 交付後,使用者補充第一點的說明:

> 「補充第一點的說明,在設定的時間之後有符合策略條件,還是要下單。
> 其它的就遵守設定的條件。」

ADR-153 的第一點原本被我讀成「**在那一刻**看一眼,不符合就今天結束」。
使用者真正要的是:**指定時刻是判斷的起點,不是唯一的一眼**。到點時條件不成立
不代表今天放棄 —— 在「持續看」的窗口內,條件一成立就要送。

「其它的就遵守設定的條件」則是把邊界劃清楚:窗口長度、每日進場上限、
交易時段閘門、五檔取價規則……這些既有設定一項都不變。

---

## 2. 查下來的實際狀況:兩條路行為不一致

| 路徑 | 到點時條件不成立 | 行為 |
|---|---|---|
| 實盤 `_qt_check_timed_entries()` | `continue`,**不** `mark_timed_done` | 窗口內每 2 秒再看一次 ✅ |
| 回測 `core/backtest.py` | **無條件** `mark_timed_done` | 一次定生死,那天不再看 ❌ |

也就是說:**實盤本來就是使用者要的行為,回測不是**。

這是 P-67(同一份規則不可以有兩份實作)的又一次:`should_fire_timed()` /
`check_timed_entry()` / `mark_timed_done()` 這三個純函式是共用的,但「什麼時候
可以收尾」這個決定被留在**各自的呼叫端**,兩邊就分岔了。分岔的後果是
**回測數字比實盤保守**:實盤會進的單,回測沒進,使用者拿一份偏少的績效
在做決策 —— 方向雖然不是「過度樂觀」那種危險,但一樣是回測與實盤對不上。

另外實盤那條路雖然行為對,**訊息是錯的**:

> 「到達指定時刻,但**當下**進場條件不成立 (A 現價 99.95),**今天不送單**」

它明明還會再看 5 分鐘,卻告訴使用者「今天不送單」。使用者照著這句話會以為
機會已經沒了。行為對、訊息錯,一樣是 bug。

---

## 3. 決定

### 3.1 收尾的唯一條件

`mark_timed_done()` **只有兩種情況**可以呼叫:

1. **真的送出去了**(委託成立或模擬記帳完成)
2. **窗口過了**(`timed_window_expired()` 為真)

「條件不成立」**不是**收尾條件。這條規則寫進 `should_fire_timed()` 的 docstring
—— 那是這三個函式的單一出處,規則寫在那裡,下一個呼叫端才看得到。

### 3.2 回測跟上

`core/backtest.py` 的 `mark_timed_done(rt, ts)` 從 `if _fire:` 底下無條件呼叫,
移進 `if _timed_it is not None:` 分支。條件不成立時改記
「窗口內下一根再看」(`collapse=True`,不會洗版)。

### 3.3 訊息改成講真話

| 位置 | 舊 | 新 |
|---|---|---|
| 條件不成立 | 「…今天不送單」 | 「…10:30 起算的 5 分鐘內會繼續看,條件一成立就送」 |
| 窗口過了 | 「常見原因:當時取不到五檔…」 | 「常見原因:**整段窗口內進場條件都沒成立**,或取不到五檔…」 |

### 3.4 UI 用詞

「有效窗口(分)」→「**持續看(分)**」。舊名字讀起來像「這段時間內指令有效」
(對應原本「時間到了那一秒沒抓到報價怎麼辦」的設計動機),新名字直接說出
它現在的作用:**這段時間內會一直看**。說明文字補上完整語意。

---

## 4. 沒有改的東西(使用者說「其它的就遵守設定的條件」)

- 窗口長度預設 5 分鐘,可自訂 —— 不變。
- 每日進場上限、冷卻秒數、熔斷 —— 不變,而且定時下單一樣要過 `risk_check()`。
- 交易時段閘門(ADR-124)—— 不變。
- 五檔取價、讓價檔數、取不到就不送單 —— 不變。
- ADR-153 的「用當下價判斷」與「記帳用預估成交均價」—— 不變,而且現在
  **窗口內每一次重問都用那一刻的當下價**,語意反而更完整了。
- 一天最多送一次 —— 不變(靠 `mark_timed_done`,不是靠風控;見下面的突變測試)。

---

## 5. 驗證

### 5.1 單元測試 `tests/test_core.py::TestTimedEntryKeepsWatching`(8 案)

| 案例 | 守什麼 |
|---|---|
| `conditions_met_after_the_specified_time_still_enters` | 10:30 只跌 0.10%、10:33 跌到 1.00% → **要有一筆進場**,落在 10:30 之後、窗口之內 |
| `still_enters_at_the_specified_time_when_already_met` | 正向對照:到點就成立的照舊在 10:30 進場,不可以被這次改動延後 |
| `one_entry_per_day_even_though_it_keeps_watching` | 反向對照:「持續看」不可以變成「持續買」 |
| `marking_done_is_what_stops_the_repeat_not_the_risk_cap` | 把上一條的斷言壓在**收尾邏輯**上而不是借風控的力(見 5.3) |
| `gives_up_when_the_window_passes` | 反向對照:10:45 才成立(超過 10 分窗口)就是今天不送 |
| `conditions_never_met_no_entry` | 反向對照:整天不成立就是不下單(使用者第 1 點的原意) |
| `log_says_it_will_keep_watching` | 訊息不可以講成「今天不送單」 |
| `should_fire_timed_stays_true_until_marked` | 單元層級的支點:沒收尾之前每一次問都要回 True |

### 5.2 診斷 `diag_repro_issues.py`(ADR-154 案例)

守**實盤**那一半(單元測試守的是回測那一半):走完整條
`_qt_check_timed_entries()`,確認 10:30 不成立時 `timed_done_day` 沒被寫進去、
10:33 成立時真的下單、10:36 不會重複送、窗口過了會收尾並講一次。
最後一項是原始碼層級斷言:回測那一半的 `mark_timed_done` 必須在
`if _timed_it is not None:` 之內(P-67 的守門員)。

### 5.3 突變測試(9 項全紅)

單元測試 5 項:

| 突變 | 結果 |
|---|---|
| 回測還原「無條件 mark_timed_done」(本次修的 bug) | FAILED ✅ |
| `should_fire_timed` 拿掉窗口上界 | FAILED ✅ |
| 送出後不 mark_timed_done | FAILED ✅(**第一版沒抓到,見下**) |
| 訊息改回「今天不送單」 | FAILED ✅ |
| 條件判斷永遠成立 | FAILED ✅ |

診斷 4 項:

| 突變 | 結果 |
|---|---|
| 實盤條件不成立時就 mark_timed_done | FAIL ✅ |
| 實盤訊息改回「今天不送單」 | FAIL ✅ |
| 回測退回一次定生死 | FAIL ✅ |
| 實盤窗口過了不收尾 | FAIL ✅ |

**第一版有一項漏掉**:`one_entry_per_day` 用的策略設 `max_trades_per_day=1`,
所以就算送出後忘了 `mark_timed_done`,重複的那幾筆也會被**風控**擋掉 ——
斷言看起來綠,實際上完全沒有守住定時下單自己的收尾邏輯。突變測試當場證明
(拿掉 mark_timed_done 仍然全綠),補了
`marking_done_is_what_stops_the_repeat_not_the_risk_cap`(每日上限放寬到 5)
才真的壓在正確的地方。這正是 P-94 講的:「跑了都沒紅」不是證明。

### 5.4 完整離線驗證

| 項目 | 結果 |
|---|---|
| `python tests/test_core.py` | **972** 案(ADR-153 後 964,+8) |
| `python tests/test_brokers.py` | 42 案 |
| `python diag_repro_issues.py` | **75** 案例(+1),0 FAIL / 0 ERROR |
| `python diag_crossref.py` | 乾淨 |
| `py_compile` | 全通過 |

---

## 6. 請使用者實機驗證

1. **到點不成立時,手機收到的訊息要說「會繼續看」** —— 而且那句話要講得出
   起算時刻與剩餘分鐘數。
2. **窗口內條件成立時真的會送出去** —— 這是這一筆的主訴求。建議把「持續看」
   先設長一點(例如 10~15 分),觀察一天。
3. **一天仍然只送一次** —— 送出後窗口還沒過的那幾分鐘,不可以再送第二筆。
4. **整段窗口都不成立時,窗口過了要收到一則「今天沒送成」** —— 而且原因
   要講得出是「條件沒成立」還是「取不到五檔」。
