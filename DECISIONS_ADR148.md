# ADR-148:券商連線中斷不可以把策略關掉,也不可以報成「合約解析失敗」

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-121(建立「外部環境問題 vs 策略邏輯壞掉」的分野)、ADR-060、
> ADR-108(Telegram 通知)、P-05、P-64、P-129、P-130
>
> **編號說明**:本筆併入 `main` 時,`main` 上的 P-128 已被另一份 ADR-147
> (定時下單)用掉,所以本筆新增的兩條坑取 **P-129 / P-130**(併入前在工作
> 分支上暫編為 P-130 / P-131)。取號時檔名與程式碼註解兩處都查過。

---

## 起因:使用者手機收到的一串訊息

使用者提供三張 Telegram 截圖(16:44 → 16:47)。表面上是 8 則不同的錯誤,
查下來**根因只有一個**:

| 時間 | 訊息 | 性質 |
|---|---|---|
| 16:44 | 「多-5分K」「空-5分K」連 3 次抓不到 K 線,`ShioajiTimeoutError` kbars 30 秒逾時 | 連線開始不穩 |
| 16:47 | 「多-5分K」`ShioajiConnectionError: kbars: Shioaji error Session error SolClient send request api/v1/data/kbars, code: WouldBlock ... "_solClient_waitForSessionEstablished for session '(c6,s1)_sinopac', timed out waiting for connection in progress"` | **根因:交易階段連線斷了** |
| 16:47 | 「5-60-空15分」`合約解析失敗: TXF`<br>「空-5分K」`合約解析失敗: MXFR1`<br>「5-60 多15分」`合約解析失敗: TXF` | **後果,不是獨立問題** |

`TXF` 與 `MXFR1` 兩個代號本身都合法 —— `_resolve_futures_contract()` 支援裸代號
轉 R1 連續合約,而且同一分鐘「多-5分K」還成功用 MXFR1 記了一筆模擬單 @ 44240。
**不是策略設定有問題。**

---

## 查出來的三個缺陷

### 缺陷 A:訊息指向錯的地方

合約表是**登入時下載、掛在連線物件上**的。`_mark_session_dead()`(ADR-060)
偵測到斷線後會在背景對舊連線做 `logout()`,合約表隨之失效 —— 這時**任何合法
代碼都會解析失敗**。

而 `_quant_eval_pass()` 的「已登入」檢查在 **for 迴圈之外**,只擋得住「進來時
就沒連線」,擋不住**中途斷線**。於是同一輪剩下的每一檔策略照樣跑下去,各報一則
「執行商品 (做B) 合約解析失敗: XXX」。

這些訊息是**我們自己製造的**,而且會把使用者導去檢查一個沒問題的代碼。

### 缺陷 B:連線抖三下會把所有策略關掉(最嚴重)

合約解析失敗原本拋普通 `RuntimeError`,走通用 `except Exception`:

```python
rt['error_count'] += 1
if rt['error_count'] >= 3:
    s['enabled'] = False      # ← 自動停用
```

ADR-121 早就為「抓不到 K 線」建立了分野,`KBarsFetchError` 的說明寫得很清楚:

> 兩者混在同一個錯誤計數裡,會讓一次網路瞬斷把一個**持有部位**的策略自動停用,
> 連帶把即時停損也一起關掉。

**合約解析失敗是同一類東西,但被漏掉了。**後果:券商連線抖三下 → 所有策略被
自動停用 → 而且**連線恢復後不會自己開回來**,使用者要手動一檔一檔開,
而他人在外面看手機。

這次只到「第 1 次錯誤」還沒觸發,但條件已經齊了。

### 缺陷 C:一個根因 → N 則手機推播

`log_message()` 只要看到 `【自動交易` 前綴就推播一份。一次斷線,策略數是幾檔就
推幾則。手機被洗版的實際後果是**真正重要的那一則被淹掉** —— 跟 P-05「失敗要
看得見」是同一個目標,只是反方向的失敗。

---

## 決定

### 1. 新增 `TransientEnvError` 基底型別,`ContractResolveError` 掛在底下

```
TransientEnvError            外部環境的暫時性問題,不是策略壞掉
├─ KBarsFetchError           (ADR-121)
│   ├─ KBarsPending          (ADR-122)
│   └─ KBarsUnavailable      (ADR-122)
└─ ContractResolveError      (ADR-148) ← 新增
```

`except` 子句改認**基底型別**。這樣下次再多一種外部性錯誤,掛在基底底下就自動
受保護,不必記得回來改那一行 —— 合約解析失敗當初被漏掉,正是因為 except 綁死在
`KBarsFetchError` 上。

### 2. 合約解析失敗一律當「暫時性」處理:重試、**永不自動停用**

「代碼打錯」與「連線沒了」在錯誤現場長得一模一樣,系統當下無法分辨。
兩種猜法的代價完全不對稱:

| 猜法 | 猜錯的代價 |
|---|---|
| 猜「設定錯了」→ 自動停用 | 網路瞬斷把使用者**所有**策略關掉,持倉的停損也跟著關掉,而且不會自己恢復 |
| 猜「連線問題」→ 繼續重試 | 代碼真的打錯就一直重試,但**抓不到合約時本來就不會下單**,成本只是日誌吵一點 |

所以一律當暫時性處理。連續失敗到一定次數(每 5 次)**明確提醒**「若券商連線
正常,請檢查商品代碼是否正確」—— **提醒,不停用**。

### 3. 訊息分流:處置相同,說法不可以相同

合約解析失敗與抓不到 K 線走同一條處置路徑(重試、不停用),但訊息必須分開:

- 說成「抓不到K線」→ 使用者往資料源查,而真正該看的是連線。
- 說成「代碼錯了」→ 使用者去改一個沒問題的設定。

實際文案:

> 【自動交易-資料異常】策略「X」連 N 次執行商品(做B)合約解析失敗: TXF ——
> 已自動重試,策略維持啟用 (解析不到合約時本來就不會下單)。合約表是登入時
> 下載、掛在連線上的,**連線中斷會讓所有代碼都查不到**,不一定是代碼寫錯。

### 4. 連線在同一輪中途斷掉 → 整輪停手

在 `for s in self.strategies` 的**迴圈內**補上 `if not api_logged_in: break`。
斷了就整輪停手,下一輪自然被迴圈外那道檢查擋掉,不再對後面的策略噴誤導訊息。

### 5. 同一個根因短時間內只推一則(120 秒窗口)

純邏輯放 `core/telegram_notify.py`(零 tkinter/shioaji,離線可測),
狀態(dict)由 GUI 層持有 —— 與 `ai_helper.py` / `order_intent.py` 同一個分層理由。

指紋算法:剝掉「策略名稱」→ 數字換空白(「第 1 次」「第 2 次」同組)→ 收斂
空白 → 取前 `BURST_KEY_LEN=45` 個字元。同一根因的訊息只差在**策略名稱**(被引號
剝除)與**商品代碼**(落在尾端被截斷切掉),剩下的「類別 + 例外型別 + 錯誤語意」
正好是要分組的東西。

用使用者截圖裡的三則原文實測:合併成 1 則;而 kbars 逾時那則**不會**被合併進去。

#### 兩條紅線

1. **成交訊息一則都不能少。**`ALWAYS_SEND_TAGS = ('【自動交易-實單', '【自動交易-模擬', '【自動交易-保護')`
   永遠逐則送出 —— 那是「錢動了」的紀錄,不是狀態提醒。「策略已自動停用」也在
   名單裡:使用者必須知道有東西被關了。
2. **被壓下的則數不可以安靜消失。**下一則同 key 通過時附上
   「(過去 120 秒內另有 N 則同類訊息已合併)」。這裡吞的是**重複**,不是資訊。

---

## 我改了一條既有的斷言 —— 要講清楚

ADR-121 的診斷案例 5b 原本是這樣寫的:

```python
app._qt_resolve = lambda _s: (None, None)     # 合約解析失敗 → 邏輯錯誤
...
assert st6['enabled'] is False, "策略邏輯連續 3 次錯誤仍應自動停用"
```

它拿**合約解析失敗**當「策略邏輯錯誤」的替身,連註解都寫明了這個等式。
ADR-148 查出**那個等式本身就是 bug**,所以這條案例在改完之後當場變紅。

這不是「改測試讓它過」。這條案例要守的**意圖沒有變**(自動停用的保護沒有被整個
拿掉),變的是替身:改成真正的策略邏輯錯誤(`_qt_fetch_closed_bars` 拋普通
`ValueError`)。合約解析失敗的正確行為由 ADR-148 的新案例守,而且**在 ADR-121
的案例裡補了一條 5c 反向對照**,直接釘住「合約解析失敗不可以停用策略」。

挑替身時踩到一個小坑:第一版改成 patch `evaluate_strategy`,但 `_reset()` 建的是
**日K** 策略,取資料那一步會先走進 ADR-122 的背景預抓並拋 `KBarsPending`,
根本到不了 `evaluate_strategy`。替身必須放在「合約解析成功之後、而且真的會被
執行到」的位置。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/strategy_engine.py` | 新增 `TransientEnvError` 基底、`ContractResolveError`;`KBarsFetchError` 改繼承基底 |
| `core/telegram_notify.py` | 新增 `burst_key()` / `always_send()` / `should_send()` / `ALWAYS_SEND_TAGS` / `BURST_WINDOW_SEC` / `BURST_KEY_LEN` |
| `stock_app_pro.py` | 三處合約解析改拋 `ContractResolveError`;`except` 改認 `TransientEnvError`;`_qt_on_kbars_fetch_error` → `_qt_on_transient_env_error`(含訊息分流);迴圈內補中途斷線 `break`;`log_message()` 接上通知合併 |
| `tests/test_core.py` | +11 案(`TestTransientEnvError`、`TestTelegramBurstThrottle`) |
| `diag_repro_issues.py` | +1 案例(ADR-148);修正 ADR-121 案例 5b 的替身、新增 5c 反向對照 |

---

## 驗證

離線(全部通過):

- `python tests/test_core.py` → **904 案**(ADR-147 後是 893,+11)
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → **70 案例**(+1)
- `python diag_crossref.py` → 無跨模組斷鏈或重複定義
- `py_compile` 全過

---

## 請使用者實機驗證

1. **連線中斷時,策略不可以被自動停用。**最容易製造的情境:量化交易開著,
   拿同一組帳號去永豐官網或 App 登入(會踢掉 API 階段,見 P-25)。
   預期:狀態列變「🔴 連線中斷」,系統日誌出現「連 N 次...合約解析失敗 ——
   已自動重試,**策略維持啟用**」,而且**策略清單裡每一檔都還是啟用狀態**。
2. **訊息不可以說成「抓不到K線」**,而且要有那句「合約表是登入時下載、掛在
   連線上的,連線中斷會讓所有代碼都查不到,不一定是代碼寫錯」。
3. **同一輪不可以每檔策略各噴一則合約解析失敗** —— 連線判定中斷後應該就停手了。
4. **手機通知**:上述情境下,同類訊息 120 秒內只應收到 **1 則**;
   下一則通過時會附「(過去 120 秒內另有 N 則同類訊息已合併)」。
5. **反向對照,這條最重要**:合併不可以吃掉成交通知 —— 讓策略在模擬模式下連續
   成交幾筆,**每一筆都要收到獨立的手機訊息**,一則都不能少。
6. **反向對照**:重新登入後,策略應該恢復正常評估(不需要手動重開任何策略)。
7. 若你確實把某檔策略的商品代碼打錯,連線正常時應該每 5 次收到一則
   「請檢查這檔策略的商品代碼是否正確。策略仍維持啟用」—— 提醒但不停用。
