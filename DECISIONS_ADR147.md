# ADR-147:定時下單 —— 到指定時刻用當時的五檔進場(零股專用解法)

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:鐵則 4(五檔絕不捏造)/ 鐵則 5(snapshots 節流)/ 鐵則 6(零股只能限價)/
> 鐵則 9(送出前本地驗證)/ 鐵則 3(報價暫存經 quote_lock)/ 鐵則 8(每路訂閱各自記成敗)/
> ADR-066(進出場時間窗)/ ADR-124(時段閘門)/ ADR-144(分批進場)/ ADR-145(即時進場)/
> ADR-146(零股機制稽核)/ P-04 / P-06 / P-67 / P-90
> 新增坑:P-128

---

## 起因

> 「零股也可以在指定時間下單(比如12:01分),零股沒有K線圖,那可以抓當時的
> 五檔即時報價快照的價格嗎?然後,有設定可以議價幾檔。」

逐項查證,結果是**一件已經有了、一件不能照原本設想的方式做、一件現在其實是壞的**。

---

## 查證結果

### ✅ 一、「議價幾檔」——已經有了

`slippage_ticks`(策略編輯器的「讓價檔數」)。`order_intent.apply_slippage()`
買進往上讓、賣出往下讓,而且**對齊台股合法檔位**(鐵則 7,不是無腦 `.2f`)。

### ⚠️ 二、五檔快照——**不能用 `snapshots()`**,這是硬限制

| | 事實 |
|---|---|
| `snapshots()` 給的不是五檔 | 只有**最佳一檔**(`buy_price`/`sell_price`)加成交價 |
| **`snapshots()` 的價永遠是整股價** | **P-06 早就記過**:shioaji **沒有**零股 snapshot,也沒有盤中零股歷史 tick 查詢 API |
| 真正的零股五檔唯一來源 | **串流**:`quote.subscribe(…, QuoteType.BidAsk, intraday_odd=True)` |

而那路串流原本**只訂閱主畫面正在看的那一檔**:`_resubscribe_quotes_worker`
換股就退訂,`on_bidask_stk_v1` 更是寫死 `if bidask.code == self.current_contract.code`
才收。所以**策略標的的五檔根本收不到**。

### ❌ 三、指定 12:01 ——欄位有,但**永遠不會成立**

`specific_entry_time` 早就存在(ADR-066),但 `filter_intents_by_time()` 比對的是
**K 棒時間戳**:

```python
if sp_entry and bar_time_str != sp_entry:
    blocked = True
```

5分K 的時間戳是 12:00 / 12:05…,**永遠不等於 12:01**。使用者填 12:01 等於這檔
策略永遠不進場,而且是**安靜的**(只留在 `time_window_skips` 裡)。只有週期選
1分K 才剛好對得上——但那是巧合,不是設計。

---

## 決定

### 用時鐘觸發,不用 K 棒時間戳

新的 `should_fire_timed(strategy, runtime, now_dt)` 看的是**時鐘**,所以任何分鐘數
都成立,也不必把週期改成 1分K 去遷就它。

觸發條件刻意是「**已經到了、而且還沒過太久**」而不是「等於 12:01:00」:
即時通道的節拍是秒級,不可能剛好命中某一秒。窗口預設 5 分鐘,可設 1~60。

> 沒有上界的話,中午 12:40 才把 App 打開會**立刻補送一張 12:01 的單** ——
> 那個決策的價格前提早就不存在了。這是使用者選的做法(另一個選項是「當天
> 一直重試到成交為止」)。

### 兩個議價旋鈕,可以疊加

使用者要的是兩個都有:

| 旋鈕 | 意思 | 出處 |
|---|---|---|
| **五檔基準(第 N 檔)** | 我願意**吃到第幾層掛單**。買進吃「賣N」、賣出吃「買N」 | `quote_book.pick_price()`(新) |
| **讓價檔數** | 在那個基準上**再多給一點價差** | `order_intent.apply_slippage()`(既有) |

兩者都是「越大越積極」,方向一致。範例:`買進 + 第3檔 + 讓價2檔`
= 用賣三的價當基準,再往上讓 2 個 tick。

### 取價規則獨立成 `core/quote_book.py`

零 tkinter、零 shioaji(鐵則 11)。GUI 只負責把券商推來的物件攤平成標準 dict:

```python
{'bid': [(價, 量), …],   # 由高到低
 'ask': [(價, 量), …],   # 由低到高
 'ts': 收到的時間, 'symbol': 代碼, 'odd': 是不是零股}
```

規則本身離線就測得完,而它決定的是「一張真錢的單用什麼價送出去」——
那正是最需要測得到的部分。

**四道刻意的拒絕**(全部回 `None` 讓呼叫端不要下單):

1. **補 0 的檔位截斷**。券商在檔位不足時補 0,那些 0 不是「價格是零」,
   是「沒有這一檔」;留著就會挑到 `0.0` 送出一張 0 元的委託。
2. **掛單不夠深就不下單**,不退回最佳檔充數。使用者選第 5 檔是
   「我願意追到第 5 檔」,只有 3 檔時用第 1 檔的成交機率完全不同。
3. **五檔太舊不採用**(預設 10 秒)。沒有時間戳也一律當不新鮮 ——
   否則「串流從沒推過的空殼」會跟「剛更新」變成同一件事。
4. **資料不一致就不下單**(賣二比賣一低之類)。正常的 book 一定滿足遞增/遞減,
   所以這道檢查平常擋不到任何東西;它擋的是陣列沒對齊、買賣接反這種
   **資料本身壞掉**的情況——那時候取值會取到離譜的價然後真的送出去。

### 取不到五檔時:**不送單**,絕不退回 K 棒價

這是整筆最重要的一條。零股的 K 棒是**整股**的價(ADR-146 記過的既有近似),
拿它去下零股限價單就是**錯價的真錢單**——而定時下單只有一次機會,
沒有下一根 K 棒可以修正。

取不到就記日誌、**窗口內下一個 tick 再試**;窗口過了才收尾並講一次話。
(鐵則 4 的精神:沒有真實資料的檔位絕不捏造。)

### 為策略標的常駐訂閱五檔

`_qt_sync_timed_subs()` 只替「**已啟用 + 有開定時下單**」的策略訂閱,停用的
不佔配額。存進**策略專用**的 `_qt_books`,與 `current_bidask_odd`(主畫面那一檔)
完全分開——P-04 的教訓就是共用暫存會互相污染。讀寫一律經 `quote_lock`(鐵則 3),
每一路訂閱各自記成敗(鐵則 8)。

**換股不可以誤殺它**:`_resubscribe_quotes_worker` 原本會把離開那一檔的四路
全退掉,剛好是策略標的的話,訂閱記錄還在、資料卻再也不來,而且完全無聲,
一直到 12:01 才發現「取不到五檔」。現在那一路 BidAsk 會跳過(Tick 照退,
策略不看 Tick)。

### 沒有進場條件也能存檔

`validate_strategy` 原本要求「至少一個進場條件」。「12:01 幫我買一張」是完全
合理的策略,不該被逼著隨便掛一個條件湊數——**湊出來的條件會真的影響下單**。
有設條件的話仍然要成立才送:定時下單是多一個**觸發時機**,不是繞過條件。

### 每一道既有的閘門都還在

| 閘門 | 定時下單 |
|---|---|
| 交易時段閘門(ADR-124) | ✅ 同一道 `market_session.is_market_open()` |
| 風控(每日上限/冷卻/熔斷) | ✅ 同一份 `risk_check()` |
| 分批進場上限(ADR-144) | ✅ 同一份 `can_scale_in()` |
| 零股只能限價(鐵則 6) | ✅ 走同一條 `build_live_order()` |
| 數量 1~999 股(ADR-146) | ✅ 同一道送出前守門 |
| 預設值 | **關閉**。會下單的功能不可以預設打開 |

### 不打任何 API(鐵則 5)

五檔來自串流快取,進場條件用 `_quant_eval_pass` 已經快取好的已收盤 K 棒
(P-90:這條路跟即時停損共用節拍,阻塞會拖住停損)。所以它掛在 runner 的
**2 秒**節拍上而不是 3 秒的 snapshots 節流裡——「指定時刻」本來就該用最密的
節拍去逼近,而它不花任何流量。

---

## 刻意不做的事

- **期貨**不在這一筆的範圍。期貨五檔走 `on_bidask_fop_v1`,而且合約代碼是
  前綴比對(`_fop_code_match`,R1 連續合約推的是實際月份),誤配的風險要另外
  處理。定時下單目前只支援**股票 / 零股**,期貨策略會被 `_qt_timed_targets()`
  跳過。
- **不改 `specific_entry_time`**。那個欄位的語意是「K 棒時間戳等於某個時刻」,
  拿掉會動到既有策略的行為。新功能是獨立的一條路,舊欄位維持原樣。
- **出場**不做定時。這次只做進場。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/quote_book.py` | **新增**:五檔正規化 `make()` / 取價 `pick_price()` / 新鮮度 `is_fresh()` / `clamp_level()` / `describe()` |
| `core/strategy_engine.py` | `timed_entry`/`timed_entry_time`/`timed_entry_window_min`/`timed_book_level` 欄位;`should_fire_timed()`、`timed_window_expired()`、`mark_timed_done()`、`check_timed_entry()`;`validate_strategy` 放行「只有定時下單、沒有進場條件」並驗證新欄位;runtime 加 `timed_done_day` |
| `stock_app_pro.py` | `_qt_timed_targets()`/`_qt_sync_timed_subs()`/`_qt_store_book()`/`_qt_get_book()`/`_qt_check_timed_entries()`/`_qt_timed_log_once()`;`on_bidask_stk_v1` 收策略標的的五檔;`_resubscribe_quotes_worker` 不誤退常駐訂閱;runner 掛上這條路;編輯器 row 17~19 |
| `tests/test_core.py` | +19 |
| `diag_repro_issues.py` | ADR-147 案例(走完整下單路徑,15 段) |
| `PITFALLS.md` | P-128 |

**編號檢查**:本地與 `origin/main` 都查過(檔名 + 程式碼註解兩處)。
目前最大 ADR-146、最大 P-127。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **884 通過**(原 865,+19)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **69 案例全 PASS**(原 68,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

### 突變測試(純函式 8 項)

| 把程式改成 | 結果 |
|---|---|
| 買賣兩邊接反(買進去看買價) | 單元紅 ×5 |
| 檔位不夠深時退回最佳檔充數 | 單元紅 ×2 |
| 補 0 的檔位不截斷(會送 0 元委託) | 單元紅 |
| 沒有時間戳就當新鮮 | 單元紅 |
| 窗口無上限 | 單元紅 |
| 定時下單預設打開 | 單元紅 ×10 |
| 定時下單繞過進場條件 | 單元紅 ×5 + 錯誤 ×2 |
| 定時下單繞過分批上限 | 單元紅 |

### 突變測試(GUI 接線 8 項)

| 把程式改成 | 結果 |
|---|---|
| callback 不存策略的五檔(訂閱成功卻收不到) | 診斷紅 |
| 取不到五檔就退回 K 棒/整股價 | 診斷紅 |
| 換股時照樣退掉策略的常駐訂閱 | 診斷紅 |
| 定時下單繞過風控 | 診斷紅 |
| 定時下單繞過交易時段閘門 | 診斷紅 |
| runner 不呼叫定時下單 | 診斷紅 |
| 五檔取價不看使用者填的檔數 | 診斷紅 |
| 不為策略標的訂閱五檔 | 診斷紅 |

診斷含**反向對照**:沒開定時下單的策略完全不受影響、也不會為它佔訂閱配額;
換股時該退的 Tick 還是要退;12:00:59 不送;當天只送一次。

### 請使用者實機驗證

1. 策略編輯器應看得到 **「⏰ 定時下單」** 勾選框(在「⚡ 即時進場」下面),
   **預設沒有勾**;底下有「指定時刻 / 有效窗口(分) / 五檔基準」三欄。
2. 開一檔**零股**策略:數量 500 股、勾定時下單、指定時刻 `12:01`、
   五檔基準 1、讓價檔數 0,**先用「模擬」模式**跑一天。
3. 09:10 之後、12:01 之前,系統日誌應出現
   `【定時下單】已訂閱 2330 的零股五檔 (常駐)`。
   **這一行沒出現就代表後面一定不會送單**,請先回報。
4. 12:01 一到,日誌應出現
   `【自動交易-模擬】🧪 …買進 500 2330 @ 100.5 [定時下單 12:01:00,賣1檔 | 買[…] 賣[…]]`
   —— 括號裡會把**當下的五檔整組印出來**,方便你核對價格是不是真的取自賣一。
5. **把主畫面切去看別檔股票**,再讓它到 12:01 —— 應該**照樣送得出來**
   (這是「常駐訂閱」要保證的事;修之前換股就會失效)。
6. 五檔基準改成 3 → 成交價應該變成**賣三**的價。再把讓價檔數設 2 →
   應該在賣三的基礎上再往上 2 個 tick。
7. **反向對照**:把時刻設成一個已經過去的時間(例如現在是 13:00,設 12:01)
   → 應該**不會補送**,而是在日誌說「已超過 5 分鐘窗口,今天不再送」。
8. 沒勾「定時下單」的既有策略,行為應該**完全不變**。
9. 確認模擬沒問題再開實單。

### 誠實說明:沒辦法離線驗證的部分

- **零股五檔串流在你的 shioaji 版本上實際推不推、推多密**。診斷用的是假的
  BidAsk 物件;`intraday_odd=True` 的 BidAsk 訂閱在盤中零股時段(09:10–13:30)
  應該有資料,但**這只能實機確認**。若那檔零股當下根本沒人掛單,五檔就是空的,
  程式會照設計不送單並記日誌 —— 那是正確行為,不是 bug。
- **shioaji 的訂閱配額**。常駐訂閱會多佔名額;策略開很多檔時會不會撞到上限,
  離線量不出來。訂閱失敗會記日誌(鐵則 8),請留意
  `【定時下單】訂閱 XXX 五檔失敗`。
- **12:01 的實際延遲**。runner 是 2 秒節拍,理論上 12:01:00~12:01:02 之間送出,
  但真實的網路來回時間離線測不到。
