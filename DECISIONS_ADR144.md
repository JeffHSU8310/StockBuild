# ADR-144:分批進場 / 加碼(拆掉「買了就不准再買」)+ 回測的「訊息 / 紀錄」分頁

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:ADR-035(狀態機)/ ADR-039(回測)/ ADR-043 / ADR-059・061(買進持有)/
> ADR-064(T+1)/ ADR-136(回測與實盤同模型)
> 新增坑:P-124

---

## 起因

使用者:

> 「回測引擎非常『死腦筋』。它的底層邏輯是:『只要你買了(狀態變成 LONG),
>  在你把股票賣掉之前,我就絕對不准你買第二筆。』,造成回測時一年只買一次的
>  元兇:純粹是因為這套回測系統的底層有著嚴格的『部位狀態機(State Machine)』
>  限制。不把股票賣掉把位子空出來(回到 FLAT 狀態),它就不准您買下一筆。
>  這個問題要修改。」
>
> 「回測系統要有『訊息 / 紀錄 / Log』標籤頁可以看。」

**診斷完全正確。** `core/strategy_engine.evaluate_strategy()` 是這樣寫的:

```python
if state == 'FLAT':
    ok, details = eval_conditions(df_closed, strategy.get('entry', []), 'AND', ...)
    ...                                  # ← 只有 FLAT 才評估進場條件
    return ...
# 持倉中:先看停損/停利,再看出場訊號   ← 這條路整段沒有「進場」
```

而回測的 `core/backtest.py` 只有**一個** `open_trade` 槽位,所以就算引擎肯發
第二個 OPEN,回測也只能記住最後一筆(而且會把第一筆的成本蓋掉)。

兩個地方都要改。

---

## 一、狀態機不拆,改成「一段部位可以由多筆進場組成」

狀態機本身是對的 —— 停損停利、風控、對帳、模擬帳戶全都靠它。要拆的是
「一段部位 = 一筆進場」這個隱含假設。

### 新設定:`max_entries`(可分批進場次數)

| 值 | 意思 |
|---|---|
| **1(預設)** | **舊行為**:買了就不准再買,一定要先平倉 |
| 2 以上 | 同一段部位最多由這麼多筆組成(分批建倉 / 加碼) |
| 0 或負數 | 不限(仍受每日進場上限、冷卻秒數、資金限制) |
| 壞值(打錯字) | **退回 1**,不是「不限」—— 一個打錯的欄位不該變成無限加碼 |

「買進持有(累積 / 定期定額)」本來就是無限累積(ADR-061/062),不受這個
上限影響 —— 那是它的定義,不是設定。

**預設 1 是整個 ADR 最重要的相容性保證**:既有策略一個字都不用改,
回測數字一模一樣。有專門的反向對照測試釘住這件事。

### 評估順序:停損停利 → 出場訊號 → 加碼

要走的時候就該走。**同一根 K 棒上不可以一邊喊出場一邊加碼** —— 有測試釘住。

### 加碼的方向必須與現有部位一致

這裡是「加碼」不是「反手」。反手是另一件事(自訂策略的
`decision_to_intents` 才做);混在一起會讓使用者在完全沒設定反手的情況下
被反向開倉。

### `apply_fill` 的成本改成加權平均(行為變更)

同方向再次成交 = 加碼:**數量累加、成本改成加權平均**,不再覆蓋。

原本只有 `buy_and_hold` 走這條;一般策略是「覆蓋」。那個行為其實是**錯的**,
只是被「持倉中不評估進場條件」的閘門遮住了 —— 一旦真的成交第二筆,覆蓋會讓
第一筆的成本基礎憑空消失,**停損停利跟著算錯**。ADR-144 開放分批進場之後這件
事會真的發生,所以統一成加權平均。

有一條測試專門驗「加碼後停損用的是均價」:1 張 100 + 1 張 200 → 均價 150,
5% 停損落在 142.5,不是 190。

> `apply_fill` 是「已經成交了,更新狀態」。「該不該買第二筆」是
> `evaluate_strategy` / `can_scale_in()` 的職責,不是它的 —— 這個分工讓
> `apply_fill` 不需要知道 `max_entries`。

### 舊 runtime 不必遷移

`entries_of(runtime)`:沒有 `entries` 這個 key 時,有部位算 1 次、沒部位算 0 次。
搭配預設 `max_entries=1`,既有策略重啟後「有部位 = 已達上限」= 舊行為。
(同 `intrabar_stop` 的處理哲學:讀取時推導,不改存檔。)

---

## 二、回測:一筆進場 = 一列交易明細

`open_trade`(單一槽位)改成 `open_lots`(清單):

- 每次進場 append 一筆 lot(自己的進場時間/價格/數量)
- 平倉時**逐筆結算**,產生各自獨立的交易明細列
- 浮動損益、期末結算也都逐筆算

使用者因此看得到「我到底買在哪幾個價位、各賺賠多少」,而不是一個被平均掉的
數字。這也是 ADR-061 累積買進早就在做的事,現在一般策略共用同一個模型。

---

## 三、「訊息 / 紀錄」分頁 —— 它要回答的是「為什麼」

這個分頁的價值不是「有紀錄」,而是回答使用者真正的問題:
**「為什麼一年只買一次?」**

所以引擎會把「進場條件當根成立、卻被擋下」的原因寫進
`runtime['blocked_entry']`,回測逐根收進紀錄:

```
2026-01-06   進場     買進 @ 103 (第 1 筆) — 進場: …
2026-01-07   未進場   (連續 36 根) 已持倉,且「可分批進場次數」是 1 ——
                      要先平倉才能再進場。想分批/加碼請把它調大。
2026-02-09   期末結算 未平倉 1 筆以最後收盤價 139 結算
```

**那一行字就是使用者花時間才推理出來的答案。** 現在打開分頁就看得到。

### 三個刻意的設計

1. **連續重複的原因會合併成一列並累計次數**。十年 5分K 有二十幾萬根,
   逐根印「已持倉」會有幾萬列 —— 使用者反而看不到重點。合併後是
   「(連續 237 根) …」,一眼就懂。
2. **上限 5,000 列,超過就截斷並在最後一列明說**,不是靜默丟掉。
3. **一筆交易都沒有時更要說話**:紀錄會寫「整段回測沒有任何一根 K 棒讓進場
   條件成立 —— 條件可能太嚴,或資料範圍不含符合的行情」,而不是給一個空白分頁。

分頁本體有種類篩選(只看「未進場」/ 只看成交)與「複製全部訊息」。

### 順帶修好一個 mock 缺口

`diag_mock_tkinter` 的 `ttk.Notebook` 原本直接指向 `_MockWidget`,**沒有 `add()`**
—— 任何用到分頁的視窗一開就 `AttributeError`。補上 `add/tab/tabs/select/index`。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/strategy_engine.py` | `max_entries` 欄位、runtime 的 `entries`/`blocked_entry`、`max_entries_of()`/`entries_of()`/`can_scale_in()`、持倉中評估加碼、`apply_fill` 改加權平均 |
| `core/backtest.py` | `open_trade` → `open_lots`;逐筆結算;`LOG_MAX_ROWS`、`_log()`、`result['log']` |
| `stock_app_pro.py` | 兩個編輯器加「可分批進場次數」;報告視窗改用 `ttk.Notebook`;新增 `_qt_build_backtest_log_tab()` |
| `diag_mock_tkinter.py` | `ttk.Notebook` 補上分頁 API |
| `tests/test_core.py` | +21 |
| `diag_repro_issues.py` | ADR-144 案例;ADR-059/061 案例的「一般策略是覆蓋」改成新行為 |
| `PITFALLS.md` | P-124 |

**編號檢查**:遠端 `origin/main` 也查過了(上一輪就是因為只查本地而撞號)。
目前最大 ADR-143、最大 P-123。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **839 通過**(原 818,+21)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **65 案例全 PASS**(原 64,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| 持倉中還是完全不評估進場(回到舊行為) | 單元紅 ×6 + 診斷紅 |
| `max_entries` 壞值退回「不限」 | 單元紅 |
| 加碼時覆蓋成本而不是加權平均 | 單元紅 ×5 |
| 出場那一根也允許加碼 | 單元紅 |
| 不記錄被擋原因 | 單元紅 + 診斷紅 |
| 平倉只結算第一筆 lot | 單元紅(**補了「走出場訊號那條路」的測試之後**才抓得到 —— 第一版的部位是被期末結算收掉的,完全沒經過 CLOSE 分支,見 P-124)|
| 報告視窗不建訊息分頁 | 診斷紅 |

診斷含**反向對照**:預設 `max_entries=1` 時回測必須仍然只買一次
(少了這條,把上限寫死成「不限」也會綠);`max_entries=3` 時的擋下原因
必須是「已達上限」而不是「要先平倉」。

### 請使用者實機驗證

1. **開任一策略編輯器 → 應該看得到「可分批進場次數」欄位**
   (內建在「每日進場上限」附近;自訂在風控那一列)。
   旁邊有一行說明:`1=買了就不准再買(要先平倉);填 2 以上可分批建倉/加碼;0=不限`。
2. **拿你那檔「一年只買一次」的策略,先不要改任何設定直接回測** →
   結果應該跟以前**完全一樣**(這是相容性驗證,很重要)。
3. **打開報告的「📋 訊息 / 紀錄」分頁** → 應該看到
   「(連續 N 根) 已持倉,且『可分批進場次數』是 1 —— 要先平倉才能再進場」。
   這就是你原本要推理才知道的原因。
4. **把「可分批進場次數」改成 3,再回測一次** → 交易次數應該明顯變多,
   交易明細會出現同一段部位的多筆進場(進場價各不相同)。
5. 檢查**加碼後的停損**是不是照均價走(交易明細的進場價 + 出場原因)。
6. 分頁上的「只看」下拉可以篩「未進場」/「進場」/「加碼」/「出場」;
   「📋 複製全部訊息」可把整份紀錄貼出來。

### 一件沒有做、要讓你知道的事

**實盤路徑也吃這個設定**(`evaluate_strategy` 是共用的),所以把
「可分批進場次數」調大之後,**實單模式也會真的加碼**。這是刻意的 ——
回測與實盤用同一個模型才有意義(ADR-136 的教訓)。
但請先用**模擬**模式跑過一天再開實單:加碼會放大部位,每日進場上限與
冷卻秒數仍然有效,但資金控管要自己算。
