# ADR-145:即時進場(用即時報價判斷「當日漲跌幅」,不等 K 棒收盤)

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:ADR-136(回測與實盤同模型)/ ADR-143(當日漲跌幅條件)/ ADR-144(分批進場)/
> ADR-087・135(即時停損)/ ADR-124(時段閘門)/ 鐵則 5 / 鐵則 14
> 新增坑:P-125

---

## 起因

使用者確認 ADR-143 的條件已完成後補充,而且加了兩個字:

> 「看A做B,當A(比如:加權指數)盤中**即時**跌幅超過X%,就買進B。」

查證後的落差是實在的:

- ADR-143 的條件本身沒問題,但引擎的**進場**判斷只在 **K 棒收盤**時發生
  (`evaluate_strategy` 拿的是 `df_closed`,而且會剔除還沒完成的最後一根)。
- 即時報價那條路(`_qt_check_realtime_futures_stops`)**只做出場**。
  它甚至只替「已經有部位」的策略把商品放進監看名單 —— 所以即時通道
  **天生只能出場,不可能進場**。

也就是說,A 選 1分K 時最快也要等最多 1 分鐘。使用者選了「用即時報價」這條路。

---

## 設計:即時判斷是 K 棒判斷的**超集**

這是整筆改動的核心原則,也是它能與回測共存的唯一理由。

即時價**不是**另一套判斷,而是「當日又多了一個成交價」:

```python
vals = [今天每一根已收盤 K 棒的 Low]      # 原本就有的
if live_price is not None:
    vals.append(live_price)               # ADR-145 加的
當日跌幅 = (min(vals) / 昨收 - 1) × 100
```

由此得到兩個性質:

1. **只會更早,不會更寬鬆。** K 棒模式會成立的訊號,即時模式一定也成立;
   即時模式成立的訊號,等那根 K 棒收盤後 K 棒模式也會成立(因為觸價會落進
   該根的 Low/High)。
2. **回測不用改。** 回測仍用當根 K 棒的高低點模型 —— 那本來就是「盤中觸價」
   的離線等價物。回測與實盤不同模型 = 回測數字是假的(ADR-136 的教訓)。

---

## 哪些條件可以即時、哪些不行

只有 **`day_drop_over` / `day_rise_over`**(當日漲跌幅)。它們只需要
「昨收 + 現在的價」。均線、KD、MACD 那些一定要 K 棒序列,即時價給不出來。

進場條件是 **AND** —— 只要**有一條**不能即時評估,整檔策略的進場就退回
K 棒模式,而且**會在系統日誌講一次原因**:

```
【即時進場】策略「XXX」沒有啟用:這些進場條件需要完整的 K 棒序列,
無法只憑即時價判斷:均線黃金交叉 (…) —— 這檔策略的進場仍會等 K 棒收盤。
```

靜默失效是不可接受的:使用者會以為即時進場在跑,其實沒有。

`check_realtime_entry()` 裡**還有一道**明確的 `if t not in LIVE_ENTRY_CONDITIONS:
return None`。這不是重複 —— 見下面 P-125 的說明:少了它,安全性是靠
「不認得的條件剛好算出 False」這個巧合在撐。

---

## 安全性:每一道既有的閘門都還在

這條路會**下單**,所以刻意做成與既有即時停損 **同一套骨架**:

| 閘門 | 即時進場 |
|---|---|
| 交易時段閘門(ADR-124) | ✅ 同一道 `market_session.is_market_open()` |
| 風控(每日進場上限 / 冷卻秒數 / 熔斷) | ✅ 同一份 `risk_check()` |
| 分批進場上限(ADR-144) | ✅ 同一份 `can_scale_in()` |
| 看A做B 的價格分離(ADR-075) | ✅ 訊號用 A、成交價用 **B** |
| 模擬 / 實單 | ✅ 同一套記帳與下單路徑 |
| 預設值 | **關閉**。會下單的功能不可以預設打開 |

**取不到 B 的價時絕不送單**,更不會拿 A 的指數點數當 B 的成交價 ——
那是這個功能最嚴重的失效模式(拿 22,000 點去下一張 600 元的股票)。
會記日誌說明並在下一輪重試。

### 不自己抓 K 線(P-90)

A 的已收盤 K 棒由 `_quant_eval_pass` 順手快取進 runtime(`_live_a_df`)。
即時路徑跟即時停損共用同一個 tick,**阻塞會拖住停損**。所以這條路只做
「快取的 df + 現在的價」的純計算,零 I/O。

### 不多打 API(鐵則 5)

沿用 `_qt_update_realtime_pnl` 那一次已經打過的 `snapshots()` 結果。
A(例如加權指數)只是在同一批請求裡多一個代碼 —— 沒開這個功能的策略
**完全不會**為它多問一次(有診斷斷言守著)。

### 自訂策略刻意不提供

自訂策略的進場邏輯寫在 `on_bar()` 裡,而 `on_bar` 天生就是「一根 K 棒
呼叫一次」的模型,沒有「用即時價重算一次」的語意。硬加會變成一個
勾了卻不會動的框。

---

## 使用方式

策略編輯器 → 勾 **「⚡ 即時進場 (用即時報價判斷,不等K棒收盤;僅「當日漲跌幅」類條件)」**

建議搭配:

```
看A做B          ✔
A(看A商品)      ^TWII          A 週期 = 1分K
B(symbol)       要下單的商品     週期  = 1分K
進場條件         當日跌幅≥X% (盤中,相對昨收)
出場訊號         當日漲幅≥Y% (盤中,相對昨收)
⚡ 即時進場       ✔
```

A 的週期仍然重要:它決定「昨收與當日極值多久更新一次」(1分K = 每分鐘),
即時價只補上那一分鐘內的變化。

> **出場**目前仍是 K 棒收盤判定(停損停利已經有 ADR-136 的即時觸發可勾)。
> 「出場訊號也要即時」是另一件事,這次沒做。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/strategy_engine.py` | `day_pct_change(..., live_price=)`;`LIVE_ENTRY_CONDITIONS`、`realtime_entry_enabled()`、`live_entry_supported()`、`check_realtime_entry()`;`new_strategy()` 加 `realtime_entry` |
| `stock_app_pro.py` | `_qt_update_realtime_pnl` 把 A 加進監看名單;新增 `_qt_check_realtime_entries()`;`_quant_eval_pass` 快取 A/B 的 df;內建編輯器加勾選框 |
| `tests/test_core.py` | +13 |
| `diag_repro_issues.py` | ADR-145 案例(走完整即時通道) |
| `PITFALLS.md` | P-125 |

**編號檢查**:本地與 `origin/main` 都查過。目前最大 ADR-144、最大 P-124。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **852 通過**(原 839,+13)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **66 案例全 PASS**(原 65,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

診斷走的是**完整即時通道**(`app._qt_update_realtime_pnl()`),而且前提斷言
明確驗過「純 K 棒模式此刻**不**成立」—— 否則這個案例證明不了「即時」這件事。

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| 即時價不納入當日極值(等於功能沒開) | 單元紅 ×3 + 錯誤 ×2 + 診斷紅 |
| 即時進場預設打開 | 單元紅 |
| 拿掉「條件必須可即時」的守門 | 單元紅(**補強測試資料之後**才抓得到,見 P-125) |
| 即時通道不呼叫進場檢查 | 診斷紅 |
| 拿 A 的價當 B 的成交價 | 診斷紅 |
| 即時進場繞過風控 | 診斷紅 |

診斷含**反向對照**:沒開這個功能的策略完全不受影響、也不會為它多打一次 A
的報價;即時價沒跌夠不進場;市場關閉不進場;取不到 B 的價不送單。

### 請使用者實機驗證

1. 策略編輯器應看得到 **「⚡ 即時進場」** 勾選框(在「可分批進場次數」下面),
   **預設沒有勾**。
2. 照上面的建議設定一檔(A=^TWII 1分K、B=你的商品 1分K、進場條件「當日跌幅≥X%」),
   **先用「模擬」模式**跑一天。
3. 盤中觀察系統日誌:訊號成立時應出現
   `【自動交易-模擬】🧪 策略「…」買進 1 2330 @ 600 [即時進場,A=^TWII 22000]`
   —— 括號裡會同時寫出 **B 的成交價**與 **A 的即時價**,方便你核對。
4. **故意把進場條件加一條均線類的** → 日誌應出現「【即時進場】…沒有啟用:…」
   並退回 K 棒模式(不是靜默不動)。
5. 沒勾「即時進場」的既有策略,行為應該**完全不變**。
6. 確認過模擬沒問題再開實單。

### 誠實說明:沒辦法離線驗證的部分

真實的 `snapshots()` 節奏(實際多久回一次)、加權指數的即時 `close` 欄位在
你的 shioaji 版本上是不是就是指數點數 —— 這兩件事只能實機確認。診斷用的是
假的 API 物件。
