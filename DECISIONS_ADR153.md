# ADR-153:定時下單看「當下」不看「曾經」;成交價用預估均價不用掛價

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-147(定時下單/`quote_book`)、ADR-149(零股取價)、ADR-151(回測實作
> 定時下單)、ADR-143/145(當日漲跌幅、即時進場)、ADR-035(樂觀成交模型)、P-06
> 新增坑:P-134

---

## 使用者回報(附 Telegram 截圖)

```
【自動交易-模擬】🧪 策略「0050零股 (大盤跌 0.2%) 買進」買進 501 0050 @ 103.05
[定時下單 10:30:00, 賣3檔 |
 買 [102.9x7261 102.85x16519 102.8x20027 102.75x7813 102.7x28368]
 賣 [102.95x13542 103x14338 103.05x10165 103.1x4095 103.15x1731]]
 | 定時進場: 進場條件成立: 當日跌幅 ≥X% (盤中,相對昨收) …
```

> 1. 「當我有設定什麼時候下單,就要以這個時間去看 A 有沒有符合策略條件,
>    若沒有符合,就不會下單。」
> 2. 「賣3檔的意思是,因為是用限價單,允許掛單價可以用高於目前賣價 3 tick 去掛單,
>    但實際成交價應該不會就是高三檔的價格,所以真實的成交價是多少?訊息要跟我說。」
> 3. 「我實際看真實交易,在這個時間成交的價格落在 102.9。」

**三點裡有兩個真 bug**,而且第 1 點還牽出第三個。

---

## 缺陷 A:定時下單判斷的是「今天曾經」,不是「這個時刻」

`day_pct_change(use='盤中觸價')` 的語意是「當日到目前為止的**最低價**」——
它的 docstring 寫得很清楚:

> 今天只要曾經跌超過 X%,這件事就成立了…它是一個**狀態**,不是事件。

那個語意對 K 棒路徑是對的(「跌深買進」的字面意思就是這樣)。但**定時下單**是
使用者指定「在 10:30 看一眼」——

> 早上 09:30 大盤跌了 0.5%,10:30 已經回到 -0.05%,舊行為**照樣送單**。

這正是使用者說的「就要以這個時間去看 A 有沒有符合,若沒有符合,就不會下單」。

## 缺陷 B:記進帳的是**掛價**,不是成交價

「賣三檔」的意思是「我最多願意付到賣三」,**不是**「我會成交在賣三」。限價買單
送出去之後是從**賣一開始往上吃**。用使用者截圖裡的真實五檔實算:

| | 價 |
|---|---|
| 掛價(賣三檔) | 103.05 |
| **實際成交均價** | **102.95**(賣一 13,542 股,買 501 股連賣二都吃不到) |

使用者觀察到的真實成交在 102.9 附近 —— 更靠近 102.95,離 103.05 很遠。

差 0.1 元 × 501 股看起來很小,但系統採**樂觀成交模型**(ADR-035):記進帳的價格
就是後續**所有**損益、停損停利的基準。基準價一開始就偏高,整條線都跟著偏。

## 缺陷 C(順帶查到):關掉「即時進場」會讓定時下單靜默失效

`rt['_live_a_df']` **只在 `realtime_entry_enabled` 時才快取**。使用者若關掉即時
進場,`check_timed_entry` 拿到 `df_closed=None` → 直接回 None → **完全不下單,
而且沒有任何訊息**。定時下單與即時進場是兩個獨立功能,不該有這種隱性相依。

---

## 決定

### 1. 定時下單一律用「觸發當下的價」評估當日漲跌幅類條件

`check_timed_entry(..., live_price=)`:對 `LIVE_ENTRY_CONDITIONS`(當日漲跌幅類)
強制改用 `use='收盤價'` 語意 + 帶入當下價;其餘條件(均線、KD…)本來就只能看
已收盤 K 棒,照舊。

**這是刻意讓同一個條件在兩條路上有兩種語意**,因為觸發時機本來就不同:

| 路徑 | 問的問題 |
|---|---|
| K 棒 / 即時進場 | 今天**曾經**跌過 X% 嗎(狀態) |
| **定時下單** | **此刻**跌著 X% 嗎(事件) |

K 棒路徑的「盤中觸價」語意**沒有被改掉**,有反向對照測試釘住。

當下價的來源:`_qt_update_realtime_pnl` 的那一批 snapshots 多帶一個 A 的代碼
(幾乎零成本,鐵則 5);拿不到就退回 `_live_a_df` 最後一根收盤價(1 分 K 落後
不到一分鐘)。

**不成立時要講一次話**:一天一次的機會沒動作,不可以靜默 ——
「到達指定時刻,但**當下**進場條件不成立 (A 現價 xxx),今天不送單」。

### 2. 新增 `quote_book.walk_fill()`:把限價單「吃檔位」算出來

回傳加權平均成交價、成交量、吃了哪幾檔、量夠不夠吃滿。

- **記帳用預估成交均價**(模擬與實單都是)—— 那是樂觀成交模型下最誠實的估計。
- **送給券商的限價仍是掛價**(那才是「我最多願意付到這裡」),兩者不可混用。
- 使用者選第 N 檔就**只吃到第 N 檔**,不會偷偷吃更貴的。
- 五檔量不夠吃滿時**明說**「只夠成交 N 股,其餘要等後續掛單」,不假裝全部成交。

#### 誠實說明:這是估計,不是保證

- 盤中零股是**集合競價**,實際成交價由撮合決定,可能比這裡更好
  (使用者看到的 102.9 就比估計的 102.95 更好)。
- 五檔是快照,送出到撮合之間掛單量會變。
- 所以這個估計刻意**偏保守**(假設你一路吃上去),**不會低估你要付的價**。

### 3. 訊息要講得出「怎麼算的」

新格式:

```
… 買進 501 0050 @ 102.95 [定時下單 10:30:00
 ｜掛價 103.05 (賣3檔)
 ｜預估成交均價 102.95 (102.95x501)
 ｜實單的**實際成交價以券商回報為準**        ← 只有實單模式才有這一句
 ｜買[…] 賣[…]]
```

`@` 後面那個數字從此是**預估成交均價**(記進帳的那一個),掛價另外標。

### 4. `_live_a_df` 也為定時下單策略快取

一行修正,解掉缺陷 C。

---

## 刻意沒有做的事

- **實單不改成「等券商回報才記帳」。**那是 ADR-035 樂觀成交模型的根本改動,
  影響每一條下單路徑,要另開 ADR。這一筆只把估計值改成**對的估計值**,
  並在訊息標明「實際成交價以券商回報為準」(真實成交價在「我的已成交」清單裡)。
- **不改 K 棒路徑的「盤中觸價」語意。**那是「跌深買進」的字面意思,而且改掉會
  動到所有既有策略的回測與實盤行為。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/quote_book.py` | 新增 `walk_fill()` / `describe_fill()` |
| `core/strategy_engine.py` | `check_timed_entry()` 新增 `live_price`,當日漲跌幅類條件改用「當下」語意 |
| `stock_app_pro.py` | `_live_a_df` 也為定時下單快取;snapshots 監看名單納入定時下單的 A;`_live_a_price` 存進 runtime;定時下單改用預估成交均價記帳、訊息改寫、條件不成立要出聲 |
| `tests/test_core.py` | +12(`TestWalkFillAndTimedNow`,用使用者截圖的真實五檔) |
| `diag_repro_issues.py` | +1 案例(ADR-153) |

---

## 驗證

- `python tests/test_core.py` → **964 案**(ADR-152 後 952,+12)
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → **74 案例**(+1)
- `python diag_crossref.py` → 無跨模組斷鏈
- `py_compile` 全過

突變測試 **5 項全紅**:吃檔位不看掛單量、買賣方向接反、吃超過使用者選的檔位、
均價改用掛價(就是這次的 bug)、定時下單改回「今天曾經」語意。

用**使用者截圖裡的真實五檔**實算:

| 情境 | 結果 |
|---|---|
| 掛價(賣三檔) | 103.05 |
| 買 501 股的預估成交均價 | **102.95**(全部在賣一) |
| 買 20,000 股 | 102.966(賣一 13,542 + 賣二 6,458) |
| 買 40,000 股 | 102.996,且標明「只夠成交 38,045 股」 |
| 當下只跌 0.05%(曾經跌 0.5%) | **不下單**,並記錄原因 |
| 當下跌 0.5% | 下單,理由標「[當下 -0.50%]」 |

---

## 請使用者實機驗證

1. **重新啟用那檔 0050 零股策略**,10:30 的訊息應該變成
   `@ 102.95 …｜掛價 103.05 (賣3檔)｜預估成交均價 102.95 (102.95x501)`。
2. **模擬帳戶的成本價**應該是 102.95 而不是 103.05。
3. **當下不符合就不下單**:挑一天大盤早盤跌超過 0.2%、10:30 已經拉回的日子,
   應該收到「到達指定時刻,但**當下**進場條件不成立 (A 現價 xxx),今天不送單」。
4. **把「即時進場」取消勾選**,定時下單仍要正常運作(這是缺陷 C 的修正)。
5. **實單模式**:訊息要有「實際成交價以券商回報為準」,而且送到券商的限價
   仍然是掛價(賣三檔),不是均價 —— 可以在「我的委託單」對照。
6. **反向對照**:一般 K 棒路徑的「跌深買進」不可以有任何行為變化
   (它仍然是「今天曾經跌過就算」)。
7. 把五檔基準改成 1 檔再跑一次 → 掛價與均價應該都變成賣一。
