# ADR-150:回測要照策略的風控設定跑 —— 每日進場上限真的生效,不論幾分 K

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-035(樂觀成交模型 §7 風控)、ADR-062(Buy & Hold 累積加碼)、
> ADR-064(T+1 成交模型)、ADR-065(冷卻只管進場)、ADR-144(回測訊息紀錄)、
> P-67
> 新增坑:P-132

---

## 使用者回報

> 「經回測後,我預設每天只進場一次,但是適用 1 分 K 週期,變成每 1 分就重新
> 計算,導致一天只要達到條件,就一直重複買進,這不符合我的需求。
> **不論是幾分 K,只要是在一天之中,有成交就算數,就不能再重複買進。**」

附的策略截圖:`每日進場上限 = 1`、`冷卻秒數 = 10800`、`可分批進場次數 = 2`、
勾選「買進後持有不賣」+「累積加碼」、勾選「即時進場」、週期 1 分 K。

---

## 根因:回測**從來沒有呼叫過 `risk_check`**

`risk_check()` 是每日進場上限、冷卻秒數、每日虧損熔斷的唯一實作。實盤那三條
下單路徑都會呼叫它,但**回測沒有** —— 它直接:

```
evaluate_strategy() → apply_fill()
```

中間沒有任何一道風控。而且 `_relax_realtime_guards()` 還把設定值改掉:

```python
def _relax_realtime_guards(s):
    s['max_trades_per_day'] = 10 ** 9
    s['cooldown_sec'] = 0
    s['daily_loss_limit'] = 0
```

原本的註解寫著:

> 回測不套用「每日次數/冷卻/熔斷」等即時風控(那是防人為狂點與當日風險控制,
> 回測要看的是策略訊號本身的績效);**若要看含風控的結果,未來可加參數**。

那個判斷在「風控 = 防手殘」的前提下說得通。但**每日進場上限不是防手殘,是策略
規則** —— 使用者用它表達「我一天只買一次」,那會**大幅改變績效**。忽略它的後果
不是「看得比較純」,而是:

> **回測數字與實盤行為對不上,而回測的用途正是預估實盤。**

實測(使用者的設定、1 分 K、270 根):

| | 一天的進場次數 |
|---|---|
| 修正前 | **397 次** |
| 修正後 | **1 次** |

實盤那一邊一直是對的(`risk_check` 有被呼叫),所以這是**回測單方面的偏差**——
使用者是拿一份過度樂觀的回測在做決策。

---

## 決定

### 1. 回測預設**照策略設定跑**,與實盤同一份 `risk_check`

`run_backtest(..., apply_risk_guards=True)`,預設 `True`。
每一個 OPEN intent 送進 `apply_fill` 之前都先過 `risk_check`。

要看舊的「純訊號績效」仍然拿得到:傳 `apply_risk_guards=False`。
GUI 的兩個回測入口(單一策略、策略比較)都走預設值,診斷有斷言確保沒有人
偷偷傳 `False`。

### 2. 冷卻要用**真實秒數**,不能用 K 棒序號

這是實作上最容易踩的一顆雷:回測迴圈裡的 `now_ts = float(i)` 是 **K 棒序號**,
不是時間。直接拿它餵給 `risk_check`,「冷卻 10800 秒」會被當成
**10800 根 K 棒** —— 冷卻等於永遠擋著,而且看起來像「策略不再有訊號」。

所以另外算一個 `now_clock = ts.timestamp()`(真實 epoch 秒)給 `risk_check` 與
`apply_fill` 用。兩者**必須用同一個時鐘**:`apply_fill` 寫的是
`last_order_ts`,`risk_check` 拿 `now - last_order_ts` 比冷卻,時鐘不同就完全
對不起來。

`now_ts`(序號)在別處還有用途(`bars_held`、`entry_i`),所以維持不動,
只多一個時鐘給風控用。

### 3. 換日重置抽成 `strategy_engine.reset_daily_counters()`

`evaluate_strategy()` 一直有做換日重置,但**回測跑自訂策略時根本不經過它**
(那條路直接呼叫 `custom_strategy.run_on_bar`)。少了這一份,`trades_today`
會一路累加,跑個幾天之後每一筆進場都被「已達每日進場上限」擋掉 ——
而且看起來像是策略不再有訊號。

抽成共用函式後兩邊呼叫同一份(P-67:同一份規則不可以有兩份實作;
少寫一份的後果不是程式壞掉,是**行為悄悄不一樣**)。

### 4. 被風控擋下要記進「訊息紀錄」

ADR-144 加訊息紀錄就是為了回答「為什麼一年只買一次」。風控擋單如果靜默,
使用者只會看到「交易變少了」卻不知道為什麼 —— 那是把舊問題換一個入口重演。

實測輸出(collapse 之後):

> `風控擋單 (連續 396 根) 買進 被擋 — 已達每日進場上限 1 次`

### 5. 兩個編輯器都有「回測套用風控」勾選框

使用者追加要求:「自訂策略仍然要有換日重置的機制可以勾選。」

新增策略欄位 `backtest_risk_guards`(預設 `True`),內建與自訂**兩個**策略編輯器
都有勾選框。取消勾選 = 回到舊的「純訊號績效」模式。

預設值刻意選「套用」而不是「相容舊行為」——舊行為是**回測忽略你的設定**,
那是這一筆要修的 bug,不是需要保留的相容性;舊策略檔沒有這個欄位一律視為套用。

自訂策略特別重要:它的回測路徑**不經過** `evaluate_strategy()`,換日重置與每日
上限完全靠這一筆補的那兩行。少了它,`trades_today` 一路累加,跑幾天之後每一筆
進場都被擋 —— 而且看起來像是策略不再有訊號。

### 6. 報告要講明「這次有沒有照風控跑」

總結列固定加一行:

> 本次回測**已套用**策略的風控設定:每日進場上限 1 次、冷卻 10800 秒、
> 每日虧損熔斷 0(與實盤同一份 risk_check)。

沒有這一行,使用者無法分辨「策略本來就只買一次」與「回測忽略了我的設定」——
而那正是這一筆要修的那種**看不見的落差**。

---

## 刻意沒有做的事

- **不改實盤行為。**實盤那三條路徑本來就有 `risk_check`,這一筆只補回測。
- **不改「可分批進場次數」(`max_entries`) 與每日上限的關係。**兩者是不同的旋鈕:
  `max_entries` 管「一段部位最多分幾次建倉」,`max_trades_per_day` 管「一天最多
  進場幾次」。使用者截圖是 `max_entries=2` + 每日上限 1 —— 結果就是一天一次,
  兩天才會湊滿兩筆。這是兩個設定相乘的結果,不是 bug。

---

## 這會改變既有的回測數字

**任何設了 `max_trades_per_day`、`cooldown_sec`、`daily_loss_limit` 的策略,
回測結果都會變。**方向一律是「交易變少、更貼近實盤」。

`new_strategy()` 的預設值是 `max_trades_per_day=3`、`cooldown_sec=300`,
所以**大部分既有策略都會受影響**。這是刻意的:舊數字是過度樂觀的那一份。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/backtest.py` | `run_backtest()` 新增 `apply_risk_guards=True`;迴圈內算真實時間戳 `now_clock`;OPEN 前過 `risk_check` 並記「風控擋單」;自訂策略路徑補換日重置;總結列標明風控狀態 |
| `core/strategy_engine.py` | 新增 `reset_daily_counters()` 與 `backtest_guards_enabled()`;`new_strategy()` 新增 `backtest_risk_guards`;`evaluate_strategy()` 改呼叫共用重置 |
| `tests/test_core.py` | +18(`TestBacktestRiskGuards`、`TestBacktestCustomStrategyGuards`、`TestBacktestGuardsSetting`、`TestResetDailyCounters`) |
| `diag_repro_issues.py` | +1 案例(ADR-150,守接線與預設值) |
| `stock_app_pro.py` | 兩個編輯器新增「回測套用風控」勾選框;兩個回測入口改讀策略欄位 |

---

## 驗證

離線(全部通過):

- `python tests/test_core.py` → **922 案**(ADR-149 後 904,+18)
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → **72 案例**(+1)
- `python diag_crossref.py` → 無跨模組斷鏈或重複定義
- `py_compile` 全過

用**使用者的實際設定**實測(1 分 K、每日上限 1、Buy&Hold 累積加碼、
`max_entries=2`):

| 情境 | 每日進場次數 |
|---|---|
| 修正前(`apply_risk_guards=False`) | 397 |
| 修正後 | **1**(連續 5 個交易日各 1 次,跨日正確重置) |
| 每日上限改成 2 | 每天 2 次 |
| 上限 99 + 冷卻 3600 秒 | 每天 5 次(270 分鐘的交易日 ÷ 1 小時) |

---

## 請使用者實機驗證

1. **用你那檔「0050零股(大盤跌0.2%)買進」重跑回測** —— 一天應該只進場一次。
2. **回測的「訊息紀錄」分頁**要看得到 `風控擋單 ... 已達每日進場上限 1 次`
   (會被 collapse 成「(連續 N 根)」一列)。
3. **總結列**要看到「本次回測**已套用**策略的風控設定:每日進場上限 1 次、
   冷卻 10800 秒…」。
4. **把每日進場上限改成 2 再跑一次** → 一天應該正好兩次(可以順便確認
   「可分批進場次數 2」與每日上限是兩個獨立的旋鈕)。
5. **反向對照**:把冷卻秒數改成 0、每日上限改成很大 → 交易次數應該回到很多筆
   (確認不是被無條件鎖成一天一筆)。
6. **跨日**:回測區間拉長到數天,確認**每一天**都有機會進場(不是整段只有一次)。
7. **勾選框**:兩個策略編輯器(內建與自訂)都應該看到
   「回測套用風控 (每日進場上限/冷卻/熔斷…)」,預設**已勾選**;
   取消勾選再跑一次,交易次數應該回到很多筆,而且總結列會標「未套用」。
8. **自訂策略**也要吃到這套 —— 自訂策略的回測路徑不經過 evaluate_strategy,
   是這一筆特別補的。請拿一檔自訂策略設每日上限 1,確認一天只進場一次、
   而且**每一天都還有機會進場**(不是只有第一天)。
9. 實盤行為這一筆沒有改動,但既然回測數字會變,建議你重新看一次幾檔主力策略的
   回測績效再決定要不要上實單。
