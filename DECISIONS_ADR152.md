# ADR-152:終極波段策略的每日晨間狀態報告

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-085/128/129(終極波段策略本體)、ADR-132(每日推播的做法)、
> ADR-108(Telegram 管線)、ADR-148(通知合併與永不合併名單)、P-64、P-99
> 新增坑:無(沿用既有模式)

---

## 使用者需求

> 「終極波段策略,有在啟動中,有辦法每天判斷報告目前策略的狀況,比如:目前移動
> 停利點數是多少?符合月線條件?還是符合其他的條件?或是虧損多少?符合什麼停損
> 條件?等等,你可以自己設計。然後在市場交易日,每天 09:10 用訊息通知我。」

---

## 為什麼這個策略特別需要

終極波段是**慢節奏**策略:每天只看一次日收盤決定「待確認」,**隔天中午 12:00**
才二次確認、12:01 才真正送單。也就是說:

> 使用者除了自己去翻策略清單,**沒有任何管道**知道「它今天打算做什麼」。

而且它的狀態機有好幾個彼此影響的旋鈕(X 分界、Y/S1 停損、C 啟動門檻、
F 步幅、SMA20 模式切換),光看策略清單那一行看不出「移動停利線現在移到哪了」。

---

## 決定

### 1. 報告內容(依使用者列的項目設計)

| 使用者問的 | 報告寫什麼 |
|---|---|
| 目前移動停利點數是多少 | 停利線點位、目前距離幾點、步幅 F;或「尚未啟動,還差 N 點到 C」 |
| 符合月線條件? | SMA20 值、收盤站上/跌破、差幾點;是否**已切換 SMA20 模式**(且不會切回) |
| 還是符合其他的條件? | 空手時:離 X 分界還差幾點;持倉時:離停損線還差幾點 |
| 虧損多少? | 目前損益(點數),以進場點位為基準 |
| 符合什麼停損條件? | 多單 Y / 空單 S1 的點位與距離 |
| (追加)今天會不會出手 | **待確認進場/出場** —— 「今天 12:00 若仍…,12:01 就會…」 |

實際輸出:

```
【自動交易-終極波段】終極波段-多 2026-08-07 晨間狀態
基準:2026-07-10 加權指數收盤 42,500.0 點 (本策略一切判斷都以日收盤為準,不看盤中跳動)
月線(SMA20):42,134.6 點,收盤站上月線 (365.4 點)
部位:多單 2 口,進場點位 41,600.0 點
目前損益:+900.0 點 (以 2026-07-10 收盤計)
停損條件:收盤跌破 Y=41,200.0 → 目前高於停損線 1,300.0 點
停利模式:點數移動停利**已啟動**,停利線 42,100.0 點,目前高於停利線 400.0 點 (步幅 F=100.0)
　　切換 SMA20 的條件:獲利已過 C,只要收盤站上月線就切換
待確認事項:無 (今天 12:00 沒有預定要執行的動作)
模式:實單｜參數 X=41,500.0 Y=41,200.0 C=300.0 F=100.0
```

### 2. 基準用「最近一根已收盤日K」,不用盤中即時價

這檔策略的**每一個**判斷(進場、停損、點數移動停利、SMA20 切換)都以日收盤為準。
拿 09:10 剛開盤的即時價去講「符合什麼條件」,會與策略實際的判斷不一致 ——
那是最會誤導人的一種報告。報告開頭標明基準日與基準價,一眼看得出是「截至昨收」。

附帶好處:**不打任何 API**(日K 走既有快取),所以不受流量限制、也不會因為
API 失敗而漏發。

### 3. 算式一律沿用 `on_daily_close()` 的同一份

獲利、停利線、SMA20 全部用引擎裡那一份。報告與引擎各算一份遲早分歧(P-67),
而分歧的後果是**使用者照著一份錯的報告做決定**。

### 4. 時刻:「過了就送」+ 每日去重(沿用 ADR-132)

- 預設 **09:10**(使用者指定),可在編輯器改。
- **交易日才發**:只判斷週一~五 —— 這個專案沒有國定假日行事曆
  (`market_session` 也只做到星期幾)。假日誤送一則的代價,遠小於維護一份會過期
  的假日表;而且假日沒有新日K,報告會直接顯示舊的基準日。
- **「過了時刻就送」而不是「剛好等於那一分鐘」**:程式可能在 09:10 當下沒開或
  正忙而錯過那一分鐘。錯過就整天不發,是一天一則的通知最糟的失敗模式 ——
  使用者不會知道自己漏掉了。配合每日去重,「過了就送」不會變成重複轟炸。
- **抓不到日K 就不標記已送**,下一個 tick 再試(ADR-122 的背景預抓可能還沒補完)。
  這一條有反向對照測試釘住 —— 標記成已送的話,那天就永遠不會補送。

### 5. 預設**開啟**

與 `pattern_enabled` 預設關閉的差別:型態提醒是**判斷**(會多送很多則、而且主觀),
狀態報告是**唯讀的事實陳述**,一天一則,而且這檔策略的節奏慢到使用者沒有別的
管道知道它今天打算做什麼。

### 6. 列入「永不合併」名單

ADR-148 的 burst 節流會把 120 秒內的同類訊息合併。一天一則的報告**被合併掉就是
整天沒看到**,所以 `【自動交易-終極波段` 加進 `ALWAYS_SEND_TAGS`。
有反向對照測試釘住。

---

## 刻意沒有做的事

- **不做盤中即時報告。**使用者要的是「每天一次」,而這檔策略一天也只動作一次。
- **不做其他策略的狀態報告。**條件組合策略的狀態(哪一條條件成立)每根 K 棒都在變,
  一天一則的快照意義不大;而且 ADR-137 已經有 `/strategy` 可以隨時查。
  要做是另一筆 ADR。
- **不打 API 拿即時價。**見決定 2。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/chukuangren_band.py` | 新增 `STATUS_NOTIFY_DEFAULT_TIME`、`status_notify_enabled()`、`status_notify_time_of()`、`should_send_daily_status()`、`daily_status_report()`;`default_strategy()` 新增 `status_notify` / `status_notify_time` |
| `core/telegram_notify.py` | `ALWAYS_SEND_TAGS` 加入 `【自動交易-終極波段` |
| `stock_app_pro.py` | 新增 `_qt_chukuangren_status_pass()`;掛進 runner;終極波段編輯器新增勾選框與時刻欄位 |
| `tests/test_core.py` | +19(`TestChukuangrenDailyStatus`) |
| `diag_repro_issues.py` | +1 案例(ADR-152,守接線) |

---

## 驗證

- `python tests/test_core.py` → **952 案**(ADR-151 後 933,+19)
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → **73 案例**(+1)
- `python diag_crossref.py` → 無跨模組斷鏈
- `py_compile` 全過

突變測試 **6 項全紅**:關掉開關仍照送、週末也送、同一天重複送、改成「剛好那一
分鐘才送」、資料不足也硬產報告、報告被通知合併吃掉。

---

## 請使用者實機驗證

1. **打開終極波段策略編輯器**,應該看到「📣 每日狀態報告」勾選框(**預設已勾**)
   與時刻欄位(**預設 09:10**),存檔後重開要記得住。
2. **交易日 09:10** 手機應該收到一則報告;內容要對得上你在編輯器裡設的
   X / Y(或 S1)/ C / F。
3. **一天只有一則** —— 09:10 之後不會再收到第二則。
4. **程式晚開也要補送**:某天 11:00 才開程式,當天那一則仍應該送出來。
5. **持倉時**要看得到「目前損益 ±N 點」「停利線在哪」「離停損還有幾點」;
   **空手時**要看得到「離 X 還差幾點」。
6. **有待確認事項時**(這是最有價值的一則):報告要明講「今天 12:00 若仍…,
   12:01 就會進場/平倉」。
7. **反向對照**:把勾選框取消 → 隔天不該再收到。
8. **反向對照**:一般(非終極波段)策略不會有這則報告。
