1139:             ("布林", self.var_bbw, lambda: self.trigger_redraw()),\n1143:             tk.Checkbutton(row_frame, text=name, variable=var, bg="#12161A", fg="white", selectcolor="#2A323D", command=self.trigger_redraw).pack(side=tk.LEFT, padx=(3,0))\n1700:             # 【第五輪修正】原本每拖一次滑桿就呼叫 self.trigger_redraw() 做「完整重繪」\n1720:                 self.trigger_redraw()\n2036:         tk.Button(dlg, text="確認並套用 (並記住此設定)", bg="#29B6F6", fg="black", font=('微軟正黑體', 10, 'bold'), command=lambda: [self._save_indicator_settings(), self.trigger_redraw(), dlg.destroy()]).grid(row=9, column=0, columnspan=4, pady=20)\n2054:         tk.Button(dlg, text="確認並套用 (並記住此設定)", bg="#29B6F6", fg="black", font=('微軟正黑體', 10, 'bold'), command=lambda: [self._save_indicator_settings(), self.trigger_redraw(), dlg.destroy()]).pack(pady=10)\n2611:     def trigger_redraw(self):\n