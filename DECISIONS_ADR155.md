# ADR-155:定時下單的截止點是「今天收盤」,並移除「有效窗口(分)」設定

- **狀態**:已實作,離線驗證通過,**未經使用者實機驗證**
- **日期**:2026-08-07
- **關聯**:ADR-147(定時下單)、ADR-153、ADR-154、PITFALLS P-137
- **取代**:ADR-147 的「有效窗口(分)」設定;ADR-154 對該設定的再詮釋

---

## 1. 起因

使用者對 ADR-154 的交付回了兩句話,一句定義需求,一句砍掉一個設定:

> 「到達指定時刻,但當下進場條件不成立,不會下單。但是,只要在今天收盤前,
> 符合條件,就會下單。**我要的就這麼簡單。**」

> 「我認為的 5 分鐘,是**抓報價如果有問題**,5 分鐘內沒有正常就不下單。但是,
> 如果是照你描述的,這個 5 分鐘內是**策略條件沒有成立**,今天就不下單。
> 如果是這樣,就**不需要這個 5 分鐘設定,就移除掉**。」

第二句指出的是我造成的混淆,而且指得完全正確。

---

## 2. 一個設定被拿去管兩件不相干的事

「有效窗口(分)」是 ADR-147 加的,當初的設計動機寫得很清楚:**取價的寬限期**
——「時間到了但那一秒沒抓到報價怎麼辦」。即時通道的節拍是秒級,不可能剛好命中
12:01:00 那一秒,所以要有一段「已經到了而且還沒過太久」的容許區間。

ADR-154 要讓「指定時刻之後才成立也能下單」時,我**沿用了同一個數字**去限制
「等條件等多久」。於是這個欄位同時管:

1. 取價失敗可以重試多久(原本的用途)
2. 等策略條件成立可以等多久(我加上去的用途)

這兩件事沒有任何理由用同一個數字。使用者設 5 分鐘的心智模型是第 1 件,
拿到的行為卻是第 2 件 —— 他會覺得「條件明明後來成立了,為什麼沒買」,
而設定畫面上完全看不出來為什麼。

**使用者的判斷是對的:兩個用途一旦分開,兩個都不需要這個數字。**

- 用途 2 的正確截止點是**今天收盤**(市場的自然邊界,不是使用者要調的旋鈕)。
- 用途 1 併進用途 2 就好:取價失敗一樣看到收盤,取到了就送,收盤還沒取到
  就是今天沒送成。不需要另設寬限期。

---

## 3. 決定

### 3.1 語意收斂成一句話

> **指定時刻起,到今天收盤前,進場條件一成立就下單(一天最多一次)。**

指定時刻是**起點**,收盤是**截止點**,中間每一個 tick(回測是每一根 K 棒)
都會再問一次。

### 3.2 移除「有效窗口(分)」

| 移除 | 取代 |
|---|---|
| `TIMED_ENTRY_DEFAULT_WINDOW_MIN` / `TIMED_ENTRY_MAX_WINDOW_MIN` | (無) |
| `timed_window_min_of()` | (無) |
| `timed_window_expired()` | `timed_day_over()` |
| `timed_entry_window_min` 策略欄位 + 驗證 + UI 輸入框 + 存檔 | (無) |

存檔時 `s.pop('timed_entry_window_min', None)` —— 舊策略檔留著這個 key 會讓
下一個人以為它還有作用。診斷有原始碼層級斷言守著它不會偷偷復活。

### 3.3 收盤時刻跟著交易種類走,而且用 `market_session` 的常數

`timed_deadline_minute_of(strategy)`:

| 交易種類 | 截止 | 來源 |
|---|---|---|
| 股票 / 零股 | 13:30 | `market_session.STOCK_CLOSE_MIN` |
| 期貨(只做日盤) | 13:45 | `market_session.FUT_DAY_CLOSE_MIN` |
| 期貨(含夜盤) | 自然日結束 | 見下 |

期貨夜盤跨過午夜,而定時下單的「今天」從 ADR-147 起就是**日曆日**
(`timed_done_day` 存 `'YYYY-MM-DD'`)。與其在這裡發明一套跨日規則跟它打架,
不如讓截止點等於自然日結束,並在程式與這份 ADR 裡寫明這個取捨。

時刻常數一律取自 `core/market_session` —— 那是「這個市場幾點開幾點收」的
單一真相來源,這裡再寫一份就是 P-67。

### 3.4 順手修掉一個放大 60 倍才會痛的洞

`_qt_timed_log_once()` 原本拿**整句訊息**去重,而訊息裡有
「A 現價 99.95」這種**每 2 秒都在變**的數字 —— 等於根本沒有去重。

窗口還是 5 分鐘時上限 150 行,勉強看得下去;改成「指定時刻到收盤」之後
就是 3 小時、幾千行。同一個洞,放大 60 倍才會痛。

改成用**穩定的原因鍵**(`'cond'` / `'no_book'` / `'no_price'` /
`'no_contract'` / `'send_fail'`)去重,並把去重狀態綁日期(換日要重新講一次,
否則昨天講過的原因今天就靜音了)。

### 3.5 訊息

| 情境 | 文字 |
|---|---|
| 條件不成立 | 「當下進場條件不成立 (A 現價 99.95) —— 10:30 起到**今天收盤前**會繼續看,條件一成立就送」 |
| 收盤了還沒送成 | 「10:30 的單今天沒送成 —— 今天已經收盤。常見原因:指定時刻起到收盤,進場條件都沒有成立,或一直取不到五檔」 |

---

## 4. 沒有改的東西

每日進場上限、冷卻、熔斷、交易時段閘門、五檔取價與讓價檔數、
ADR-153 的「用當下價判斷」與「記帳用預估成交均價」—— 一項不變。
一天最多送一次也不變(靠 `mark_timed_done`)。

---

## 5. 驗證

### 5.1 單元測試

`TestTimedEntryADR147` 新增/改寫 5 案:

| 案例 | 守什麼 |
|---|---|
| `watches_until_the_close_not_a_fixed_window` | 12:01 起一路到 13:30 都要繼續問,13:31 才收尾 |
| `day_over_never_fires_before_the_specified_time` | 還沒到指定時刻不可以宣告「今天沒送成」(見 5.3) |
| `deadline_follows_the_trade_type` | 截止點跟著交易種類走,而且用 `market_session` 的常數 |
| `futures_day_session_stops_at_1345` | 期貨日盤 13:45,不可以沿用股票的 13:30 |
| `window_setting_is_gone` | 欄位、常數、函式都要真的不存在;舊策略檔留著 key 也不可以影響行為 |

`TestTimedEntryKeepsWatching` 改寫 3 案:`enters_even_much_later_in_the_day`
(10:45 才成立照樣送)、`enters_right_up_to_the_close`(13:29 才成立照樣送)、
`does_not_enter_before_the_specified_time`(指定時刻之前成立不算)。

### 5.2 診斷(ADR-154/155 案例)

守實盤那一半,新增兩段:收盤前 5 分鐘(13:25)才成立照樣要送;
連續 5 個 tick 餵**不同**的 A 現價,「條件不成立」只可以講**一次**。
另加原始碼層級斷言:UI 不可以還在讀寫 `timed_entry_window_min`,
`timed_window_min_of` / `timed_window_expired` 不可以還被呼叫。

### 5.3 突變測試(7 項全紅)

| 突變 | 結果 |
|---|---|
| 收盤後照樣送(拿掉截止判斷) | FAILED ✅ |
| 期貨用股票的 13:30 | FAILED ✅ |
| 指定時刻前就宣告「今天沒送成」 | FAILED ✅(**第一版沒抓到,見下**) |
| `timed_window_min_of` 偷偷復活 | FAILED ✅ |
| 日誌去重改用整句訊息 | FAIL ✅ |
| 截止點退回固定的 10:40 | FAIL ✅ |
| (ADR-154 既有 4 項) | 全紅 ✅ |

**第一版有一項漏掉**:`day_over_never_fires_before_the_specified_time` 用的是
09:00,分鐘數根本沒超過收盤 —— 「已經過收盤」與「已經過指定時刻」兩個條件
在那個時間點都不成立,拿掉防線也照樣綠。要真的走到那道防線,**指定時刻本身
必須落在收盤之後**(使用者把零股設成 15:00 這種設定錯誤)。改成 15:00 之後
突變當場紅。P-94 第三次:「跑了都沒紅」不是證明。

### 5.4 完整離線驗證

| 項目 | 結果 |
|---|---|
| `python tests/test_core.py` | **978** 案(ADR-154 後 972,+6) |
| `python tests/test_brokers.py` | 42 案 |
| `python diag_repro_issues.py` | 75 案例,0 FAIL / 0 ERROR |
| `python diag_crossref.py` | 乾淨 |
| `py_compile` | 全通過 |

---

## 6. 請使用者實機驗證

1. **策略編輯器裡「有效窗口(分)」那格已經不見了**,五檔基準往左移一格。
   打開既有的定時下單策略,版面不可以錯位(P-104 撞格)。
2. **到點不成立時,訊息說的是「今天收盤前會繼續看」** —— 而且同一個原因
   一天只講一次,不會被 A 現價的跳動洗版。
3. **離指定時刻很久之後才成立,照樣會送**(這是這一筆的主訴求)。
   例如設 10:30,條件 11:40 才成立,應該在 11:40 送出。
4. **收盤了才收到一則「今天沒送成」**,而不是 5 分鐘後就收到。
