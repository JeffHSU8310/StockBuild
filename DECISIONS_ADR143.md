# ADR-143:內建條件 —— 當日(盤中)漲跌幅,配合看A做B 做「跌深買進 / 漲多賣出」

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:ADR-007(期貨交易日)/ ADR-057(條件庫)/ ADR-074・075(看A做B)/ ADR-065
> 新增坑:P-121

---

## 起因

使用者:

> 「內建條件策略,幫我新增一個。
>  一樣看A做B,當A盤中跌幅超過X%,就買進B。(支援股票整張、零股、期貨)
>  X為參數,可自行輸入。
>  當A盤中漲幅超過Y%,就賣出B。(支援股票整張、零股、期貨)Y為參數,可自行輸入。」

---

## 做法:只新增兩個「條件」,不新增策略種類

看A做B(ADR-074/075)、股票/零股/期貨(`trade_type`)、數量、委託方式、
停損停利、回測 —— 這些**既有機制全部都在**。使用者要的東西缺的只有一塊:
「A 今天跌/漲了多少」這個判斷。

所以這次**不開新的策略種類**(像終極波段那樣的 `kind`),只在 ADR-057 的
條件庫加兩個條件:

| key | 顯示名稱 | 用途 |
|---|---|---|
| `day_drop_over` | 當日跌幅≥X% (盤中,相對昨收) —— 跌深買進 | 放進**進場條件** |
| `day_rise_over` | 當日漲幅≥Y% (盤中,相對昨收) —— 漲多賣出 | 放進**出場訊號** |

這樣一來:

- **看A做B 自動成立** —— 引擎本來就把條件算在 A 的 df 上、把單下在 B
  (ADR-075),條件函式甚至不需要知道 B 存在。
- **股票整張/零股/期貨自動支援** —— 走的是既有的下單路徑,零股照樣被
  鐵則 6 強制限價、期貨照樣用期貨那組常數。
- **回測自動支援** —— 回測與實盤跑的是同一個 `evaluate_strategy`。
- 停損停利、即時觸發(ADR-136)、每日進場上限、冷卻秒數、交易時段閘門
  也全部照舊生效。

### 設定方式

策略編輯器 →
- 勾「看A做B」,A 填要觀察的商品(例如加權指數 `^TWII`),B 填要下單的商品
- 進場條件加一條 **當日跌幅≥X%**,X 自己填
- 出場訊號加一條 **當日漲幅≥Y%**,Y 自己填
- 交易種類選 股票 / 零股 / 期貨,數量照該種類的單位(張 / 股 / 口)

做空方向會自然鏡像(A 跌破 X → 以賣出進場;A 漲過 Y → 回補),
這是引擎既有的 `direction` 語意,新條件沒有動它。

---

## 三個必須講清楚的定義

### 一、「跌幅」相對什麼?→ 預設**昨收**,可改今開

台股講「漲跌幅」就是**相對前一交易日收盤價**(漲跌停也是以它 ±10%),
所以預設是昨收。參數 `參考價` 可切成「今開」。

實作上,「昨收」是**前一個交易日最後一根**的收盤價 —— 不是「前一根」。
這個區別在分K上是致命的:5分K 的「前一根」是 5 分鐘前的價格,拿它算出來的
「當日跌幅」跟使用者想的完全是兩回事,而且數字看起來很合理,他不會發現。

找不到昨收(df 只有當天)時**回 None、條件不成立**,不退而求其次拿前一根
充數(鐵則 4 的精神:沒有的東西不要編一個出來)。

> 日K 上這個定義會自然退化成「前一根收盤」—— 前一根就是前一個交易日,
> 不需要特別處理。

### 二、「盤中」是觸價還是收盤?→ 預設**盤中觸價**,可改收盤價

使用者說的是「**盤中**跌幅超過 X%」。最貼近字面的解讀是:今天只要**曾經**
跌超過 X%,這件事就成立了 —— 所以預設 `用哪個價 = 盤中觸價`,拿**當日到
目前為止的最低價**(漲幅則是最高價)去比。

它是一個**狀態**而不是事件:A 今天跌了 4%,那在 FLAT 的時候就該買。
重複進場由既有的「每日進場上限」與「冷卻秒數」控制,不在這裡再發明一套。

想要「收盤確認」的人把參數改成 `收盤價`,就變成拿當根收盤價比。

兩種都只讀**已收盤的 K 棒**(呼叫端傳進來的就是 `df_closed`),
回測與實盤看到的是同一件事,**沒有未來函數**。

### 三、期貨夜盤的「當日」不是自然日

ADR-007 早就定過:期貨 15:00 之後的夜盤歸屬**下一個交易日**。用自然日分組
的話,「當日」會從半夜被切成兩半,跌幅整個算錯。

所以 `_bar_dates()` 會依資料判斷要不要套用交易日規則。規則本身抽成
`futures_session.session_date_of()`,與 `resample_future_session()` 的向量版
**共用同一條規則**(P-67:不要兩份各自維護)。

判斷「這份資料有沒有夜盤」刻意**看資料而不是看策略設定的商品別** ——
條件函式只拿得到 df,拿不到策略;而且看資料比看設定可靠(設定可能跟實際
載進來的資料不符)。判斷本身有兩道,缺一不可:

1. **全部都是 00:00 就直接回 False**。日K/週K/月K 的索引都正規化到午夜,
   而午夜正好落在「早於 08:45」的夜盤延續時段裡 —— 少了這道,每一份日K
   都會被誤判成含夜盤。
2. 剩下的才看時間:>= 15:00 或 < 08:45 才算夜盤。股票分K(09:00~13:30)
   不會誤判。

---

## 一個刻意修掉的既有坑:門檻吃絕對值

條件庫裡既有的 `pct_change_below` 標示「單根跌幅≤指定% (填負數)」。
使用者填 3 而不是 -3 的話,條件會變成「只要不是漲超過 3% 就成立」—— 幾乎
每天都觸發,而且**沒有任何提示**。

使用者說的是「跌幅超過 X%」,X 在他腦中就是正數。所以新條件一律
`abs(value)`:填 3 或 -3 都是「跌 3%」。

> 這條的第一版單元測試是**空殼**:只斷言「填 3 與填 -3 結果相同」,而在
> 「跌 3.5%」的資料上兩者都是 True,拿掉 `abs()` 照樣綠。突變測試抓到之後
> 補上「跌 1% 的資料填 -3 也必須是 False」才真的擋得住(P-121)。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/futures_session.py` | 新增 `session_date_of()` / `has_night_session_bars()` / `NIGHT_SESSION_OPEN_MIN` |
| `core/strategy_engine.py` | 新增 `DAY_PCT_BASES` / `DAY_PCT_USES` / `_bar_dates()` / `_day_ref_price()` / `day_pct_change()` / `_c_day_drop_over()` / `_c_day_rise_over()`;註冊兩個條件 |
| `tests/test_core.py` | +17 |
| `diag_repro_issues.py` | ADR-143 案例(走完整 `_quant_eval_pass` + 回測) |
| `PITFALLS.md` | P-121 |
| `ARCHITECTURE.md` | 條件庫的說明補這一組 |

**編號檢查**:原本取 ADR-140 / P-118,推上去才發現遠端已被另一個工具
(Codex)用掉 ADR-140~142 與 P-118~120 —— 那些 commit 當時還沒 push,
本地查不到。合併時整批改成 **ADR-143 / P-121**。
教訓見 P-121 的「出處」一節:編號檢查要**連遠端一起查**。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **803 通過**(原 786,+17)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **64 案例全 PASS**(原 63,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

診斷案例走的是**完整 GUI 路徑**(`app._quant_eval_pass()`),不是直接呼叫
純函式 —— 三種商品別各跑一次「A 跌 4% → 買進 B」與「A 漲 4% → 賣出 B」,
並斷言**下單價是 B 的 600 而不是 A 的 97**(看A做B 接錯線的話會立刻紅)。
另外含回測路徑與做空鏡像。

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| 昨收改成「前一根收盤」 | 單元紅 ×9 + 診斷紅 |
| 觸價只看最後一根(盤中曾跌破會漏掉) | 單元紅 ×2 |
| 門檻不取絕對值 | 單元紅(**補強斷言後**才抓得到) |
| 夜盤不照交易日分組 | 單元紅 |
| 拿掉「日K 索引全是午夜」那道判斷 | 單元紅 |
| 兩個條件接反(漲的算跌) | 單元紅 + 診斷紅 |
| 下單價改用 A 的價 | 診斷紅 |

診斷本身也含**反向對照**:A 只動 0.3% 時不可以進場(否則把條件寫成
「永遠成立」全部照樣綠);X 拉到 5% 時跌 4% 不可以進場(證明參數真的被讀取);
回測門檻拉到 50% 不可以有任何交易。

### 一個做這個案例時踩到的坑

第一版診斷的「A 轉漲 → 出場」永遠不成立,而失敗訊息只說「應該出場卻沒
出場」。根因是引擎有「同一根 K 棒不重複評估」的閘門(`runtime['last_bar_ts']`),
而我兩種情境的**最後一根 K 棒時間戳相同**,於是第二輪整個被閘門吃掉。
修法是讓「轉漲」的資料真的多一根、時間更晚的 K 棒 —— 那也才像真實的一天。

### 請使用者實機驗證

1. 策略編輯器 → 新增策略 → 勾「看A做B」,A 填 `^TWII`(或你要看的商品),
   B 填要下單的商品。
2. 進場條件加「**當日跌幅≥X% (盤中,相對昨收)**」,X 填你要的數字(例如 2)。
   出場訊號加「**當日漲幅≥Y% (盤中,相對昨收)**」,Y 填你要的數字。
3. 交易種類分別試 **股票(張)/ 零股(股)/ 期貨(口)**,數量填對應單位。
4. 先用**回測**跑一段歷史,確認進出場點位符合你的預期
   (回測明細會印出條件的完整名稱與參數)。
5. 再用**模擬**模式跑一天,看系統日誌的進出場時間點對不對。
6. 若你要的是「收盤價確認」而不是「盤中觸價」,把條件參數的
   **用哪個價** 改成「收盤價」。
7. 若你要的基準是今天開盤而不是昨收,把 **參考價** 改成「今開」。
