# ADR-151:回測要實作「定時下單」與「交易時段閘門」;定時下單是唯一的進場觸發

> 狀態:已實作、離線驗證通過,**尚未經使用者實機驗證**
> 相關:ADR-147(定時下單)、ADR-150(回測套用風控)、ADR-146(零股 09:10 開盤)、
> ADR-072(零股開盤時刻可設定)、ADR-124(交易時段閘門)、ADR-064(T+1 成交)、
> P-06、P-64、P-132
> 新增坑:P-133

---

## 使用者回報

> 「進場時間跟我設定的不一樣。」
> 「09:03 零股也不能交易,所以,回測的數據是不是還是抓整股。」

策略設定:**指定時刻 10:30**、有效窗口 10 分、五檔基準 3、交易種類**零股**、
週期 1 分 K、每日進場上限 1。
回測結果:每一筆進場都在 **09:03**。

---

## 查出三個問題,都是同一類:**回測不知道實盤有那些限制**

這是 ADR-150 的同一個病灶再出現兩次。

### 一、回測完全不知道有「定時下單」這回事

`timed_entry` 在 `core/backtest.py` 裡**一次都沒有出現**。ADR-147 把定時下單
做在 GUI 的實盤路徑(`_qt_check_timed_entries`),回測只跑 `evaluate_strategy`,
所以照舊在「條件一成立的那根 K 棒」進場 —— 那正是 09:03。

### 二、回測完全沒有交易時段閘門

`market_session` 在 `core/backtest.py` 裡**一次都沒有出現**。盤中零股 09:10 才
開盤(ADR-072/146),09:03 的成交在實盤根本不可能發生 —— 使用者一眼就看出來了。

### 三、零股回測吃的確實是整股 K 線

使用者問「回測的數據是不是還是抓整股」——**是**。`_qt_prepare_df()` 走
`_qt_resolve()` 拿的是整股合約,而**零股本來就沒有 K 線**(P-06:shioaji 沒有
零股 K 線,也沒有零股 snapshot)。這是先天限制,不是 bug,但**必須講明**,
否則使用者會以為回測的價格就是零股的成交價。

---

## 決定

### 1. 定時下單是**唯一**的進場觸發

舊行為是「定時下單只是多一個觸發時機」。配上「每日進場上限 1」的結果是:

> 條件在 09:03 成立 → K 棒路徑先進場 → 額度用完 → **10:30 的定時單永遠送不出去**。

也就是說**定時下單實質上等於失效**,而使用者看到的是「進場時間跟我設定的不
一樣」。UI 上那句「到指定時刻用當時的五檔進場」本來就只有一種讀法。

所以啟用 `timed_entry` 時,K 棒路徑與即時進場路徑**都不產生進場單**。
出場不受影響 —— 定時下單只管進場,持倉一定要出得去。

**實作位置很重要**:抑制寫在 `filter_intents_by_time()` 裡,不是
`evaluate_strategy()` 結尾。Buy & Hold 那幾條分支會**提早 return**,自訂策略也
走自己的路,但**每一條 return 路徑都會先經過** `filter_intents_by_time()`。
放在結尾的第一版就是被 Buy & Hold 繞過去的(單元測試當場抓到)。

### 2. 回測實作定時下單

用與實盤**同一份** `should_fire_timed()` / `check_timed_entry()` /
`timed_window_expired()` / `mark_timed_done()`。窗口內、條件成立才進場,
一天一次;窗口過了記一次「未進場」訊息。

**回測無法模擬五檔** —— 沒有歷史五檔資料,所以成交價仍走既有的 T+1 開盤價
模型。實盤那邊走的是零股五檔(ADR-147/149),兩者會有價差。報告有標明。

### 3. 回測套用交易時段閘門

與實盤同一道 `market_session.is_market_open()`,同樣尊重 `session_gate`。
零股因此在 09:10 之前不會有成交。

**判斷「這份資料有沒有盤中時刻」用實際的 K 棒間距,不是策略宣告的 timeframe。**
第一版用 `timeframe_minutes()` 判斷,結果**單元測試紅了 33 個** —— 因為既有測試
餵日線給宣告「5分K」的策略,兩者不一致;而且日K 的時間戳是 00:00,拿去比
「盤中 09:00~13:30」會把每一根都判成休市,整份回測一筆交易都不會有。

會不會有「盤中第幾分鐘」這件事是**資料的性質**,不是設定的性質。改用 K 棒間距
的中位數(< 1 天 = 盤中資料)之後,既有測試**一個都不用改**。

### 4. 零股回測要標明用的是整股 K 線

總結列加一行:

> ⚠ 零股**沒有 K 線**,本次回測用的是**整股**的歷史 K 棒 —— 盤中零股是獨立
> 市場、有自己的成交價,實際成交價會與這裡不同。實盤的零股掛價走的是零股五檔
> (ADR-147/149),回測無法模擬(沒有歷史五檔資料)。

只有零股策略會出現這一行(整股策略出現就是雜訊,有反向對照測試)。

---

## 這會改變既有的回測數字

- **開了定時下單的策略**:進場時點從「條件成立的那根」變成「指定時刻」,
  交易次數通常會變少。
- **分K 策略**:非交易時段的成交會消失(零股影響最大 —— 09:00~09:10 那十分鐘)。
- 日K 類與沒開定時下單的策略**完全不受影響**。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/strategy_engine.py` | `filter_intents_by_time()`:定時下單啟用時濾掉 OPEN;`check_realtime_entry()`:定時下單啟用時直接回 None |
| `core/backtest.py` | 匯入 `market_session`;依 K 棒間距判斷是否為盤中資料;時段閘門;定時下單的觸發/窗口/收尾;零股 K 線來源的警語 |
| `tests/test_core.py` | +11(`TestBacktestTimedEntryAndSession`) |

---

## 驗證

- `python tests/test_core.py` → **933 案**(ADR-150 後 922,+11)—— **既有測試一個都沒改**
- `python tests/test_brokers.py` → 42 案
- `python diag_repro_issues.py` → 72 案例
- `python diag_crossref.py` → 無跨模組斷鏈
- `py_compile` 全過

突變測試 **6 項全紅**:K 棒路徑仍搶先、即時進場仍搶先、回測不觸發定時下單、
回測不套時段閘門、時段閘門對分K 不生效、零股警語不出現。

用**使用者的實際設定**實測(0050 零股、1 分 K、指定時刻 10:30、窗口 10 分):

| | 進場時間 |
|---|---|
| 修正前 | 09:03(每天) |
| 修正後 | **10:30**(連續 5 個交易日都是) |

零股時段閘門實測:第一筆從 09:03 → **09:10**;整股仍是 09:03(不受影響);
關掉 `session_gate` 可回退到 09:03(反向對照)。

---

## 請使用者實機驗證

1. **重跑那檔 0050 零股策略的回測** —— 每筆進場時間應該都是 **10:30**(±窗口內)。
2. **訊息紀錄**要看得到零股那句警語:「零股沒有 K 線,本次回測用的是整股的歷史
   K 棒…」。
3. **把指定時刻改成 11:00 再跑** → 進場時間要跟著變成 11:00。
4. **取消「定時下單」再跑** → 回到「條件一成立就進場」,但零股仍不該早於 09:10。
5. **反向對照**:一般整股策略的回測不可以有任何變化(除非它開了定時下單)。
6. **實盤**:定時下單現在是唯一的進場觸發 —— 開了它的策略,K 棒路徑與即時進場
   都不會再搶先開倉。請確認這正是你要的語意。
